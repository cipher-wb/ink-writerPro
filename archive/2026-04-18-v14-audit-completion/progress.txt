# Ralph Progress Log
Started: 2026年 4月15日 星期三 20时09分27秒 CST
---

## Codebase Patterns
- Pydantic v2: use `BaseModel.model_validate` / `model_dump(mode="json")` for JSON roundtrip
- Tests under `tests/harness/` run with `pytest --no-cov` for fast smoke
- Module layout: `ink_writer/<feature>/{__init__.py, models.py, <core>.py}` — mirror foreshadow/

## 2026-04-18 - US-017
- FIX-17 P4d 闭环：ink-plan 消费 propagation_debt（新卷规划把 active debts 作为硬约束注入，落盘后翻转 resolved）
- Files added:
  - ink_writer/propagation/plan_integration.py（load_active_debts / filter_debts_for_range / mark_debts_resolved / render_debts_for_plan）
  - tests/harness/test_plan_consumes_debts.py（11 tests：过滤语义 / 区间消费 / 幂等翻转 / 端到端）
- Files changed:
  - ink_writer/propagation/__init__.py（导出新 API）
  - ink-writer/skills/ink-plan/SKILL.md（Workflow 新增 Step 1.5 + 完整注入示例 + Step 8 收尾调用）
- Tests: tests/harness/ 104 passed, 1 skipped（上轮 93 → 104，+11，零回归）
- **Learnings for future iterations:**
  - acceptance 里的 status='active' 在模型里并不存在，需要解释为 open+in_progress 集合，用常量 ACTIVE_STATUSES 锁死，避免后续歧义
  - 只在 plan 落盘后调用 mark_debts_resolved，避免中断留下"状态假 resolved"的脏数据
  - SKILL.md 代码片段写 Python，而不是 bash——因为 propagation 模块无 CLI wrapper，planner 直接在其自身 Python 段内调用 API
---

## 2026-04-18 - US-014
- FIX-17 P4a foundation: 新模块 ink_writer/propagation/ + propagation_debt.json schema
- Files added:
  - ink_writer/propagation/__init__.py
  - ink_writer/propagation/models.py (PropagationDebtItem / PropagationDebtFile, Pydantic v2)
  - ink_writer/propagation/debt_store.py (DebtStore: load/save/append, default path .ink/propagation_debt.json)
  - tests/harness/test_propagation_debt_store.py (6 tests: empty-load, roundtrip, upsert, insert-new, invalid chapter, invalid severity)
- Tests: tests/harness/ 61 passed, 1 skipped (零回归)
- **Learnings for future iterations:**
  - Pydantic v2 enum via `Literal[...]` is simpler than `Enum` and serializes cleanly
  - `DebtStore(project_root=tmp_path)` pattern lets tests isolate without monkeypatching cwd
  - 目标章节/检测章节分离是 FIX-17 反向传播的核心 schema 决策——detector (下一个 US) 会消费这个字段定位回溯点
---

## 2026-04-18 - US-018
- FIX-17 P4e: propagation 全链路端到端集成测试
- Files added:
  - tests/integration/__init__.py
  - tests/integration/test_propagation_e2e.py (2 tests, @pytest.mark.integration)
- Files changed:
  - pytest.ini (testpaths 追加 tests/integration；注册 integration marker)
- Scenario: 30 章 mock 项目，第 25 章 continuity-checker 报 target=5 的 cross_chapter_conflict
  - run_propagation(ch=30, interval=30) 经真实 sqlite index.db 捕获 1 条 drift
  - 验证落盘 .ink/propagation_debt.json
  - 验证 load_active_debts / render_debts_for_plan / filter_debts_for_range 消费路径
  - mark_debts_resolved → status='resolved' + active 列表清空 + 幂等
- 零回归：pytest --no-cov 全量 EXIT=0（progress 100% 全部 passed/skipped）
- **Learnings for future iterations:**
  - drift_detector 会同时扫描 critical_issues 和 checker_results；mock 时避免重复源以防 drift 双倍
  - tests/integration 需显式加入 pytest.ini testpaths，pytest 才会自动发现
  - integration marker 需要在 pytest.ini [pytest] markers= 注册以消除 PytestUnknownMarkWarning
  - 构造真实 sqlite review_metrics 表比 records= 注入更能覆盖 _load_records_from_db 的 SQL 路径
---

## 2026-04-18 - US-021
- FIX-18 P5c: context-agent 注入 progression summary（出场角色"本章之前"多维度演进摘要）
- Files added:
  - ink_writer/progression/__init__.py（导出 build/render/DEFAULT_MAX_ROWS_PER_CHAR）
  - ink_writer/progression/context_injection.py（Protocol-based source，≤5行/角色，章节升序取最近）
  - tests/harness/test_context_progression_injection.py（9 tests：裁剪、before_chapter 过滤、空降级、真实 IndexManager 兼容性）
- Files changed:
  - ink-writer/skills/ink-write/references/step-1.5-contract.md（必填输出结构新增 character_progression_summary 字段）
  - ink-writer/agents/context-agent.md（新增 Step 4.5 构造流程 + Contract 必须字段增补 character_progression_summary）
- Tests: tests/harness 122→131 (+9)；全量 2388 passed / 1 skipped，零回归
- **Learnings for future iterations:**
  - context-agent 是 Markdown agent 规格而非 Python 模块，实际注入逻辑必须落在 ink_writer/ 下的可测试 Python 模块，agent 规格只描述"调用哪个 API"
  - 用 typing.Protocol 定义 _ProgressionSource，让 build_progression_summary 兼容任何带 get_progressions_for_character 方法的对象，便于 harness mock 不依赖 sqlite
  - 裁剪策略 "保留最近 N 条" = rows[-N:]，依赖 SQL ORDER BY chapter_no ASC 的契约，test 同时写了真实 IndexManager 兼容性用例防签名漂移
  - Contract 字段有"占位符保留"语义：character_progression_summary 空时也必须保留键，与 active_negative_constraints 保持一致，下游 Step 2A 才不会 KeyError
---

## 2026-04-18 - US-026
- FIX-11 批量迁移：`ink-writer/scripts/data_modules/` 35 模块 + 53 测试全量合入 `ink_writer/core/{state,index,context,cli,extract,infra}/`
- 执行步骤：
  1. 建 bucket 骨架 + __init__.py
  2. `git mv` 35 模块到对应 bucket（state/index/context/cli/extract/infra）
  3. `git mv` 测试目录 → `ink_writer/core/tests_data_modules/`
  4. `python3 scripts/migration/fix11_merge_packages.py --apply`（rewrite 63 files 的绝对导入 + pytest.ini）
  5. 再跑 /tmp/fixup_relative.py：把 bucket 内部的跨桶 `from .config` / `from .index_manager` 等 19 个文件的相对导入改写为 `from ink_writer.core.<bucket>.<mod>` 绝对导入
  6. 删 `ink-writer/scripts/data_modules/__init__.py`（transitional shim 立即移除，因零回归已满足）
  7. 手工修 4 个失败点：test_backup_manager 的 sys.modules patch 路径、test_encoding_validator 的 SCRIPTS_DIR、test_sql_first_flush 的源文件路径、test_fix11_script 被 script 自己改掉的 `src = "sys.path.insert(...)"` 字面量
  8. CLAUDE.md 顶部追加"Python 包统一到 ink_writer/" 说明
- Tests: 2406/2407 pass（baseline） → 2403/2422 pass + 19 skip + 0 fail（**零失败，新增 +15 测试来自 tests/migration**）
- 零回归校验：新增失败 = 0（AC 红线 5），远低于回滚阈值
- **Learnings for future iterations:**
  - `fix11_merge_packages.py` 的 `EXCLUDED_DIR_NAMES` 含 "data_modules"，所以 bucket 内部 `from .X` 相对导入**不会被 --apply 自动处理**——必须在 `git mv` 之后再单独跑一个相对→绝对 rewrite 才能让跨桶 import 通
  - pytest `pythonpath` 中保留 `ink-writer/scripts` 仍必要——该目录下还有 init_project/archive_manager 等非 data_modules 脚本；但必须**新增 `ink-writer`** 到 pythonpath 才能让 `import dashboard` 工作（原先靠 pytest rootdir 自动注入 `ink-writer/`，FIX-11 后测试移到 `ink_writer/core/tests_data_modules/` 就失去了那个 implicit 注入）
  - 迁移 script 会把正文里的 `sys.path.insert(...)` 字面量注释成 `# [FIX-11] removed: ...`——这对 tests/migration/test_fix11_script.py 的 src 赋值行是破坏性的，必须迁移后手工恢复为 `src = "..."`
  - 新增 `tests/migration` 到 testpaths 后，需要 `scripts/__init__.py` 让 `scripts.migration` 能作为包被 test 导入（test 里 `from scripts.migration import fix11_merge_packages`）
  - `fix11_merge_packages.py` 的 `RE_FROM_DM_FLAT` 仅在 all 名字都在 BUCKET_MAP 时才改写——测试里的 `from data_modules import (DataModulesConfig, EntityLinker, ...)` 混合了 class 名（非 module 名）不会被自动改，要手工重写为多行绝对导入
---

## 2026-04-18 - US-027
- FIX-11 清理旧目录 + 文档对齐（sealing PR）
- 操作：
  - 删除 `ink-writer/scripts/data_modules/`（US-026 后仅剩 __pycache__ 空骨架，未纳入 git 跟踪）
  - 验证 CLAUDE.md 已指向单包源 `ink_writer/core/`（US-026 已更新）
  - 验证 scripts/verify_docs.py 无 data_modules 路径依赖
- Tests: pytest --no-cov 全量 2403 passed / 19 skipped（零回归）
- **Learnings for future iterations:**
  - US-026 的 `git mv` 已完成真正的代码迁移，US-027 只剩 `__pycache__` 清理；ralph 工作流里这类"尾巴 story"要先确认实际残留物是否仍被 git 跟踪，否则 commit 只会是空 staging
  - CLAUDE.md 的"双目录"警示在 US-026 commit 里就已被替换为"单包源"——AC 读起来像待办，实则已完成，需以 `git show` 核实而不是字面执行
---
