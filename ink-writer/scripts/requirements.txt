# Ink Writer - Python Dependencies
# Python >= 3.12 required

# 核心依赖
aiohttp>=3.8.0          # 异步 HTTP 客户端（API 调用）
filelock>=3.0.0         # 文件锁（状态文件并发控制）
pydantic>=2.0.0         # Schema 校验

# 可选依赖（提升 RAG 中文分词质量，未安装时回退到单字分词）
# jieba>=0.42.0         # 中文分词（取消注释以启用）
