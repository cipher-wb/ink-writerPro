## Codebase Patterns
- Agent 规格文件在 `ink-writer/agents/`，技能文件在 `ink-writer/skills/`
- 审查门禁逻辑在 `ink-writer/skills/ink-write/references/step-3-review-gate.md`
- 弱动词/感官判据维护在 `ink-writer/skills/ink-write/references/prose-craft-rules.md`
- 每个 US 通常涉及 agent 规格 + review gate + references 三处联动修改
- 黄金三章（ch1-3）规则在多个 checker 中都有加严处理模式
- 感官技法参考 `ink-writer/references/scene-craft/desire-description.md`
- 电影剪辑参考 `ink-writer/references/scene-craft/combat-scenes.md`
- 句式节奏参考 `ink-writer/references/scene-craft/pacing-rhythm.md` 或 `pacing-control.md`
- 场景工艺索引 `ink-writer/references/scene-craft/scene-craft-index.md`
- ink-init 深度模式的金手指收集在 `ink-writer/skills/ink-init/SKILL.md` Step 3；Quick 模式方案表在同文件 Quick Step 1；内部数据模型/充分性闸门在文件末尾
- 修改 init 收集字段时需三处联动：Step 文字说明 + 内部数据模型 JSON + 充分性闸门；Quick 模式另需同步方案表/输出模板/Quick Step 3 映射
- ink-plan 主规格在 `ink-writer/skills/ink-plan/SKILL.md`；章纲生成规则（第 1 章闭环、爽点前置、验证 Hard fail）集中在文件后半部分（章纲 format ~ line 494；第 1 章硬约束 ~ line 532；Validation 1.7 ~ line 655；Hard fail conditions ~ line 764）
- `scripts/ralph/prd.json` 是 `/Users/cipher/ai/ink/ink-writer/prd.json` 的符号链接（macOS 大小写不敏感路径），改一处即可
- 抽象收益词黑名单（理解/领悟/感悟/知道了/发现了/明白了/意识到/成长了/坚强了/看透了/释怀了/有了新认知）与允许 payoff_form 枚举（资源获取/敌人击退/他人认可/地位提升/信息解锁/危机解除）在 init/plan/review 三处复用，后续可共享 references

---

## 2026-04-17 - US-001
- 实现：ink-init 第3波（金手指）新增爽点前置收益场景必填字段
- 变更文件：
  - `ink-writer/skills/ink-init/SKILL.md`：Deep Mode Step 3 新增 Step 3+（first_payoff ≥80字 / visual_signature ≥50字 / escalation_ladder 三段）和 Step 3++（读者爽感自检追问）；内部数据模型 golden_finger 新增 4 字段；充分性闸门新增条目 5a 硬阻断；Quick 方案表与输出模板新增「第一章爽点预览」≥50 字字段；Quick Step 3 补充字段填充规则
  - `prd.json`：US-001 passes=true
- **Learnings for future iterations：**
  - init 的充分性闸门是 plan 阶段的入口硬门禁，新增字段必须在闸门处登记为硬阻断条件，否则后续 stage 接收到空值会崩
  - Quick 模式和 Deep 模式共用同一充分性闸门，所以 Quick Step 3 必须显式补齐所有 Deep 必收字段的默认生成逻辑
  - 抽象收益词黑名单（理解/领悟/感悟/知道了/发现了/明白了/意识到/成长了/坚强了）在 US-004/US-013 也会复用，后续可抽取到共享 references
---

## 2026-04-17 - US-002
- 实现：ink-init Step 2 新增角色语言特征档案（voice_profile）必收子字段
- 变更文件：
  - `ink-writer/skills/ink-init/SKILL.md`：Deep Mode 新增 Step 2+（5字段 voice_profile + 自动推荐规则 + Y/N 确认）；内部数据模型 protagonist 与 relationship 增 voice_profile / voice_profiles 结构；充分性闸门新增 3a 硬阻断；Quick Step 3 增 US-002 自动填充段
  - `ink-writer/templates/output/设定集-主角卡.md`：新增「## 语言特征档案」段落（5 维度 + 自动生成标记）
  - `scripts/ralph/prd.json`：US-002 passes=true
- **Learnings for future iterations：**
  - 主角卡模板路径在 `ink-writer/templates/output/设定集-主角卡.md`（`docs/templates`），所有 init 字段最终都会落到此模板某个段落；新增任何字段都需要同时在模板里开槽，否则 plan/write 阶段查不到
  - voice_profile 的「自动生成 + Y/N 确认」是兜底体验，user 答「不知道」时不能阻塞流程，必须有可执行的默认推荐规则；后续 OOC checker 也会消费这些字段
  - 配角语言档案以 dict 形式挂在 `relationship.voice_profiles[<name>]`（区别于 protagonist 的 inline 结构），方便后续按角色名 O(1) 查询
---

## 2026-04-17 - US-003
- 实现：ink-init 新增第一章爽点规格 `ch1_cool_point_spec`，派生到 `golden_three_plan.json.chapters["1"]`
- 变更文件：
  - `ink-writer/skills/ink-init/SKILL.md`：新增 Step 3+++ 派生规则（scene_description/involved_characters/payoff_form/reader_emotion_target 四字段 + Y/N 确认）；内部数据模型新增 `ch1_cool_point_spec` 结构；充分性闸门 5b 硬阻断；执行生成新增「Patch golden_three_plan.json」小节（含幂等/重置规则）；Quick 模式 US-003 自动填充段（关键词匹配 payoff_form + emotion 模板）；成功标准追加 `user_confirmed=true` 校验
  - `scripts/ralph/prd.json`：US-003 passes=true
- **Learnings for future iterations：**
  - `.ink/golden_three_plan.json` 的实际生成逻辑在 `ink-writer/scripts/data_modules/golden_three.py::build_golden_three_plan`，chapter 1 的 contract 字段在 `ch1_closure_contract`；若后续 US 需要从正文检查 `ch1_cool_point_spec`，应在 `resolve_golden_three_contract` 同步透出，否则 write/review 读不到
  - init 流程中，派生字段（从其他必收字段合并生成）应显式标注 `derived_from` 字段，便于幂等重跑时判断是否需要重置 `user_confirmed`
  - payoff_form 的 5 个枚举值（资源获取/敌人击退/他人认可/地位提升/信息解锁）将在 US-004 `concrete_payoff` 与 US-013 硬阻断复用，建议后续抽取到共享 references 文件
---

## 2026-04-17 - US-004
- 实现：ink-plan 第 1 章大纲强制爽点闭环 gate，PC-1 爆发章锁定第 1 章，新增 cool_point_preview / 读者承诺金手指具体承诺句
- 变更文件：
  - `ink-writer/skills/ink-plan/SKILL.md`：
    - Step 1 必载列表新增 `.ink/golden_three_plan.json`（缺 `chapters["1"].ch1_cool_point_spec` → hard fail，阻断）
    - Step 4 爽点前置原则：PC-1 第 1 卷爆发章从"不得晚于第 3 章"收紧为"锁定第 1 章"
    - 章纲 format 后的第 1 章硬约束重写为 `爽点闭环` 三节拍（ability_demo+感官×≥2 / crisis_trigger 与金手指直接相关 / concrete_payoff 硬排除词表+允许列表枚举），新增 `cool_point_preview` ≤300 字字段与金手指具体承诺句硬要求，保留旧 `第1章闭环` 兼容映射
    - Validation 1.7 同步新增 8 条硬阻断项；Hard fail conditions 新增 9 条对应项
  - `prd.json`（符号链接 `scripts/ralph/prd.json`）：US-004 passes=true
- **Learnings for future iterations：**
  - `.ink/golden_three_plan.json` 由 US-003 派生，plan 阶段是其第一个消费者；若 `ch1_cool_point_spec.payoff_form` 不在 5 枚举内，最早在 plan 硬 fail，不会泄到 write 阶段
  - `爽点闭环` 三节拍（ability_demo/crisis_trigger/concrete_payoff）命名与 prd US-004 保持一致，US-013 golden-three-checker 将用同名字段检测正文落地，需保持术语稳定
  - 第 1 章 `读者承诺` 的"金手指具体承诺句"模板（"{金手指}将帮助主角{动词}{对象}"）会被 US-013 CH1_READER_EMOTION_PREDICTABLE 反向校验，writer-agent 也建议引用
  - 兼容策略：保留旧 `第1章闭环` 字段的交叉一致性校验，避免已写入旧格式章纲的项目在二次 /ink-plan 时被误判失败
---

## 2026-04-17 - US-005
- 实现：ink-plan 章纲新增 info_budget 信息释放配额字段（max_new_concepts / max_named_characters / setting_reveal_queue / natural_delivery_hints），第 1 章严格上限 2/2 并强制金手指优先
- 变更文件：
  - `ink-writer/skills/ink-plan/SKILL.md`：Step 7 章纲 Chapter format 追加 `info_budget` 区块 + 字段说明（5 种融入方式枚举：行动展示/对话揭示/后果倒推/误读制造/环境映射）+ 第 1 章严格执行小节；Validation 新增 1.75 节（8 条硬阻断）；Hard fail conditions 新增 7 条；Final check 与章节缺失清单的字段枚举追加 `info_budget`
  - `prd.json`（符号链接 `scripts/ralph/prd.json`）：US-005 passes=true
- **Learnings for future iterations：**
  - info_budget 的 5 种融入方式枚举（行动展示/对话揭示/后果倒推/误读制造/环境映射）将被 US-008 L11 信息密度铁律与 US-012 flow-naturalness-checker 直接消费，建议后续抽取到 `references/shared/info-delivery-methods.md`
  - 第 1-3 章 `max_new_concepts` 上限 2 是"塞信息感"的量化防线，与 US-004 `cool_point_preview` 组合后实际把第 1 章信息带宽锁死在"金手指机制 + 1 个辅助设定 + 2 个有名角色"
  - AC 中提到的 `templates/output/大纲-详细大纲.md` 模板文件并不存在（章纲文件在运行时生成到 `大纲/第{volume_id}卷-详细大纲.md`），章纲字段的"模板"实为 SKILL.md 内 Chapter format 代码块——新增字段只需在此处追加即可
  - `setting_reveal_queue` 长度 > `max_new_concepts` 是"配额超支"的典型形式，后续 writer-agent 可据此在起草前做预检
---

## 2026-04-17 - US-007
- 实现：writer-agent L10 新增 L10e 感官主导模态法则 + L10f 情绪节奏句式法则（主导感官轮换、sensory_plan、连词删除、CV≥0.40、"连续短句→突然长句"断层）
- 变更文件：
  - `ink-writer/agents/writer-agent.md`：L10 header "四条子规则"→"六条子规则"；L10d 后新增 L10e（场景配伍/轮换规则/sensory_plan/与 L10b 协同/违规示例）与 L10f（三档节奏/短句稀缺律/连词删除触发/CV 目标/自检/违规示例）完整规则段；proofreading-checker 协同拦截码新增 `SENSORY_MODALITY_STUCK` 与 `SENTENCE_RHYTHM_FLAT`；polish-agent 兜底描述升级为 "Layer 8/9"；禁止事项 L10 条款追加"相邻 2 个场景主导感官未轮换 / 句长 CV<0.35 / 紧张段通篇中长句或情感段通篇短句"；成功标准第 3 条追加 L10e/L10f 描述
  - `prd.json`：US-007 passes=true
- **Learnings for future iterations：**
  - L10e 的 `sensory_plan`（主导感官规划）与 L10d 的 `shot_plan`（镜头规划）是一对"场景内部锚定产物"，均不输出到正文；后续 US-009 Step 2B 电影剪辑适配应同时消费这两个 plan，US-010/US-011 检查器应按 plan 枚举值统计覆盖率
  - L10b（每 800 字 ≥1 处非视觉感官）是"量的底线"，L10e（场景主导感官轮换）是"质的规划"——两者不可互相替代；后续 polish-agent Layer 9b 感官注入应同时满足两个维度
  - L10f 的 CV 目标 0.40 来自标杆小说节奏分析（28 字均值），低于 0.35 判 critical；连词删除仅在紧张/战斗段允许，内心独白/回忆段禁止删连词（破坏思维连贯）——后续 proofreading-checker `SENTENCE_RHYTHM_FLAT` 规则需区分场景类型
  - `desire-description.md` 实际路径在 `skills/ink-write/references/writing/desire-description.md`（不在 `references/scene-craft/`），`pacing-rhythm.md` 在 `skills/ink-review/references/pacing-control.md`——progress.txt 原有 Codebase Patterns 行需修正
  - writer-agent 子铁律新增"五处联动"模式再次验证：header 计数 / 规则正文 / checker 码 / 禁止事项 / 成功标准；本次因一次添加 2 条（e+f），所有五处都是批量追加
---

## 2026-04-17 - US-006
- 实现：writer-agent L10 铁律下新增 L10d 电影镜头切换法则（远景/近景/特写三分镜、场景内≥1次切换、战斗场景强制三段式、连续>3段同镜头违规、起草前生成 shot_plan、与 L10b 感官协同）
- 变更文件：
  - `ink-writer/agents/writer-agent.md`：L10 header"三条子规则"→"四条子规则"；L10c 后新增 L10d 完整规则段（镜头职能分工/切换节奏/shot_plan/镜头感官协同/违规示例/参考文件）；proofreading-checker 协同拦截码新增 `SHOT_MONOTONY`；禁止事项 L10 条款补充"场景无镜头切换/连续>3段同类型/战斗缺三段式节奏"；成功标准第3条追加"镜头多样且决定性瞬间有特写（L10d）"
  - `prd.json`：US-006 passes=true
- **Learnings for future iterations：**
  - `references/scene-craft/` 下的正确文件名是 `combat.md`（不是 combat-scenes.md，后者在 `skills/ink-write/references/writing/` 下），writer-agent 内引用场景工艺应指向 `references/scene-craft/<name>.md`；progress.txt Codebase Patterns 中的 combat-scenes.md 行条目需在后续 US 中核实是否沿用旧名
  - writer-agent 的子铁律新增需"五处联动"：(1) Lxx header 计数（三条→四条）、(2) 规则正文段、(3) proofreading-checker 协同拦截码列表、(4) 禁止事项 Lxx 条款、(5) 成功标准 Lxx 描述——少一处都会造成审查器/禁止事项失同步
  - `shot_plan` 是 L10d 的内部锚定产物（不输出到正文），后续 US-009 Step 2B 电影剪辑适配可直接消费此概念；US-010 prose-impact-checker 的"镜头多样性"维度也应按 shot_plan 的三分镜枚举（远景/近景/特写）做统计
  - 战斗场景三段式"远景→近景→特写"与 L7 大卖点"铺垫→爆发→反应"天然对齐（特写=爆发、远景回收=反应），后续 polish-agent Layer 9a 可优先识别此耦合
---
