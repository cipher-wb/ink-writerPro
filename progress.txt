# Ralph Progress Log
Started: 2026-04-23 (m1-case-library-foundation)
Source PRD: tasks/prd-m1-case-library-foundation.md
Branch: ralph/m1-case-library-foundation
Prev run: chapter-word-limit-hard-cap → v23.0.0 released (字数硬上限对称阻断)

## Codebase Patterns
(承接自前轮 ralph runs，保留跨 PRD 通用条目。逐轮新增追加到下方。)

- **UTF-8 编码硬约束**：所有 `open()` / `Path.read_text()` / `Path.write_text()` 必须带 `encoding="utf-8"`。scripts/fix_utf8_encoding.py 可批量修复；scripts/audit_cross_platform.py 是 9 类风险单一扫描入口。
- **Windows 兼容守则**：新增 .sh 必须配 .ps1（UTF-8 BOM）+ .cmd 双击包装；新增带 `if __name__ == "__main__":` 的脚本在 main 开头调用 `runtime_compat.enable_windows_utf8_stdio()`（Mac no-op）。详见 CLAUDE.md。
- **agent 规格统一目录**：所有 agent 规格在 `ink-writer/agents/`（非 `ink_writer/`，US-402 已消除双目录）。
- **pytest.ini testpaths 必须显式注册**：新建 `tests/<module>/` 目录后必须追加到 `pytest.ini` 的 `testpaths` 行，否则 pytest 不收集；项目覆盖率门禁 `--cov-fail-under=70`。运行时可用 `pytest tests/<new-dir>` 直接目标调用绕过 testpaths，适合 pre-US-002 的单任务验证。
- **Python 包 vs 插件目录命名**：`ink_writer/` 是 Python 包（pyproject.toml 注册），`ink-writer/` 是 Claude Code 插件（agents / skills / scripts）；不可混用。
- **Ruff 是 lint gate**（`python3 -m ruff check`）。常见自动修复：UP009（去 `# -*- coding: utf-8 -*-`）、UP035（`typing.Sequence` → `collections.abc.Sequence`）、I001（import ordering）。新增 .py 提交前先跑 `ruff check --fix <paths>`。
- **无 mypy/pyright 配置** — PRD "Typecheck passes" 的实际含义：`python3 -m py_compile <file>` + `ruff check` 同时干净。
- **断链检测标准写法**：`p.is_symlink() and not p.exists()`（`Path.exists()` 会 follow 符号链接，所以返 False 即 target 丢失）。

## ⚠️ Long-Term Roadmap Reminder（每轮 ralph 启动新 AI 实例时必读）

本 PRD 是 **5 周 M1-M5 roadmap 的第 1 步**。完成本轮所有 user stories 后，**绝不能停下**——必须继续推进 M2-M5。

**完整 roadmap**：`docs/superpowers/M-ROADMAP.md`（cat 这个文件了解全貌）

**M1 之后下一步（给完成本 PRD 的最后一个 AI 实例 / 给打开新会话的人类）**：
1. 验证 M1 收尾：`git tag m1-foundation` + 把 `M-ROADMAP.md` 中 M1 状态从 🟢 改为 ✅ + 填完成日期
2. 启动 M2：在新会话里说"基于 docs/superpowers/specs/2026-04-23-...md §9 M2 + M-ROADMAP.md，brainstorm M2 design"
3. 走标准流程：brainstorm spec → writing-plans → /prd → /ralph → bash scripts/ralph/ralph.sh

**当前 M（M1）目标**：基础设施 + Qdrant 替换 FAISS + Preflight + symlink 修复
**下一 M（M2）目标**：场景级切片 + 病例库种子 + ≥ 100 active cases
**最终 M（M5）目标**：Dashboard + 自进化闭环 + 用户扩展接口

---

## 2026-04-23 — US-001 修复 reference_corpus 断链（方案 A 硬拷贝）
- 新增 `scripts/maintenance/__init__.py` + `scripts/maintenance/fix_reference_corpus_symlinks.py`：
  - `FixReport` dataclass（`fixed / skipped / missing_source / missing_paths`）+ `summary()`。
  - `fix_reference_corpus_symlinks(reference_root, corpus_root)` 走 `rglob("*.txt")`，用 `is_symlink() and not exists()` 判断断链 → `shutil.copy2` 从 `corpus_root / rel` 硬拷贝；真实文件跳过；源缺失记入 `missing_paths` 不中断；reference_root 不存在返空报告。
  - CLI `main(argv)` 默认 `--reference-root benchmark/reference_corpus` / `--corpus-root benchmark/corpus`；`from runtime_compat import enable_windows_utf8_stdio` 仅 Windows 生效（Mac no-op）。
- 新增 `tests/maintenance/__init__.py` + `tests/maintenance/test_fix_reference_corpus_symlinks.py`（5 用例，含 PRD 要求 3 件 + idempotent + absent-root guard）：`pytest tests/maintenance -q --no-cov` → 5 passed。
- 实跑：`python3 scripts/maintenance/fix_reference_corpus_symlinks.py` → `fixed=1487 skipped=0 missing_source=0`；二次运行 → `fixed=0 skipped=1487 missing_source=0`（幂等）。`head -c 80 benchmark/reference_corpus/诡秘之主/chapters/ch001.txt` 输出《诡秘之主》第一章正文（"痛！好痛！"）。`find benchmark/reference_corpus -type l -name '*.txt' | wc -l` → 0。
- Ruff auto-fix 后 `ruff check scripts/maintenance tests/maintenance` 干净；`python3 -m py_compile` 通过。
- 文件变更：4 个新 .py + `scripts/ralph/prd.json`（US-001 `passes: true`，通过 symlink 指向 root `prd.json`）+ 本 progress.txt。`benchmark/reference_corpus/**` 本身在 `.gitignore`，1487 个硬拷贝只落在工作区（这正是 CASE-2026-0000 的本质：它是运行期产物，不入仓）。
- **Learnings for future iterations:**
  - Patterns discovered: 见顶部 Codebase Patterns 新增 4 条（pytest 目标调用、ruff auto-fix 清单、typecheck 实际含义、断链检测标准写法）。
  - Gotchas 1：`scripts/ralph/progress.txt` 是 symlink → `../../progress.txt`（repo root）。直接写入 symlink 路径即可，内容保存在 root 文件里；git status 中出现 `modified: progress.txt` 是正常的。若未来某次 ralph 失误整体覆盖而非 append，可用 `git show HEAD:progress.txt` 恢复。
  - Gotchas 2：`benchmark/reference_corpus/**` 已在 `.gitignore`，所以 1487 个硬拷贝不会进 git——这是正确行为（数据是运行期派生物，脚本与 PRD 才是真文档）。之后每个新建 dev 环境都要先跑 `python3 scripts/maintenance/fix_reference_corpus_symlinks.py` 才能读到 corpus；preflight US-014 的 `check_reference_corpus_readable` 正是为此兜底。
  - Gotchas 3：Python 3.14 + ruff 默认开 UP035——`from typing import Sequence` 会被改成 `from collections.abc import Sequence`。新代码直接按 `collections.abc` 写。
  - Context：CASE-2026-0000 零号病例要等 US-009 才正式入库，引用的 `preflight-reference-corpus-readable` checker 在 US-014；US-001 仅修原始数据层。下一轮 ralph 应挑 US-002（pytest.ini testpaths + qdrant-client 依赖 + 空包骨架）。
---
