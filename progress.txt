# Ralph Progress Log
Started: 2026年 4月17日 星期五 16时44分59秒 CST

## Codebase Patterns
- Skill 配置文件目录：`ink-writer/skills/ink-init/`（US-402 已消除双目录）
- Quick 模式加载列表位于 `ink-writer/skills/ink-init/SKILL.md` §Quick Step 0 开头。
- 创意类参考统一放 `ink-writer/skills/ink-init/references/creativity/`，命名前缀区分层级（meta-/anti-trope-/seeds-/perturbation-/style-/golden-finger-）。
- markdown 参考文件必须带 frontmatter `name/description/type/version`；字数统计只算汉字，上下限写在 PRD 验收标准里。
- 修改 PRD 后直接把 `passes` 改为 `true`，`notes` 写量化结论便于下轮复现；Ralph 无交互。

---

## 2026-04-17 - US-001
- 新建 `ink-writer/skills/ink-init/references/creativity/meta-creativity-rules.md`（2056 汉字，10 条 M01-M10 元规则，每条 5 字段：规则名/核心定义/正例×2/反例×2/适配题材，覆盖仙侠/都市/末世/科幻/悬疑/游戏/历史/现实/恋爱 9 类）。
- 文件末尾给出方案校验硬门槛：单套 ≥3 条命中、3 套合计 ≥7 条覆盖、未通过重抽；方案标注 `meta_rules_hit` 字段。
- 修改 `ink-writer/skills/ink-init/SKILL.md` Quick Step 0 加载列表，新增该文件为 L1 必读（第 5 条）。
- Files changed: 2（新增 meta-creativity-rules.md；编辑 SKILL.md）。
- **Learnings for future iterations:**
  - Quick Step 0 加载列表的修改点在 SKILL.md 第 26-33 行附近；后续 US-002/003/004/005 要继续追加条目，保持编号递增。
  - `perturbation-engine.md`（US-003）需要引用 meta-creativity-rules.md 中的 Mxx 编号，本文件已在末尾定义 `meta_rules_hit` 字段，下游可直接对齐。
  - 验证字数用 `python3 -c "import re; print(len(re.findall(r'[\u4e00-\u9fff]', open(...).read())))"`，只算汉字，别算 markdown 符号。
  - 本 PRD 所有 US 的"Typecheck passes" 门禁对纯 markdown 文件等价于 JSON schema/frontmatter 语法正确；无需运行 mypy。
---

## 2026-04-17 - US-002
- 新建 `ink-writer/skills/ink-init/references/creativity/anti-trope-seeds-schema.json`（JSON Schema Draft-07，10 类 category enum，Seed 必填 7 字段 seed_id/category/value/rarity/genre_tags/example_pairing/source，可选 meta_rules_hit；$schema 字段允许 data 自引用）。
- 新建 `ink-writer/skills/ink-init/references/creativity/anti-trope-seeds.json`（skeleton：version=v1.0-skeleton, total=0, categories=10 类 meta, changelog=[], seeds=12 条示例跨全 10 类）。
- 新建 `ink-writer/skills/ink-init/references/creativity/anti-trope-seeds-roadmap.md`（Phase-Seed-1 分 10 批流程/稀有度分布硬约束表 rarity≥4 合计 ≥50% + rarity=5 ≥20%；Phase-Seed-∞ 触发条件 A/B/手动；OQ-10 单条贡献命令明示范围外）。
- 修改 `ink-writer/skills/ink-init/SKILL.md` Quick Step 0 加载列表第 6 条加入 anti-trope-seeds.json（L1 必读），原第 6 条题材 anti-trope-*.md 顺延为第 7 条。
- 校验：`python3 -c "import jsonschema; jsonschema.validate(data, schema); jsonschema.Draft7Validator.check_schema(schema)"` 双向通过。
- Files changed: 4（3 新增 + 1 编辑）。
- **Learnings for future iterations:**
  - JSON schema 若声明 `additionalProperties: false`，数据顶层有 `$schema` 会被判为非法；必须把 `$schema` 加进 properties 白名单。
  - genre_tags 枚举与 category 枚举是两套词典（genre_tags 里没有 mythology / taboo / worldview 等），写示例时别把 category 名误当 genre。
  - skeleton 文件的 example 种子不计入 `total`；Phase-Seed-1 启动时 `total=0 → 100 → ... → 1000` 与 example 物理分离，避免 Ralph 越权注入正式种子跳过 review。
  - jsonschema 依赖在系统 Python 3.14 已安装（`/Library/Frameworks/Python.framework/Versions/3.14/`），后续 US-014 测试可直接 import。
  - Quick Step 0 加载列表当前编号：1 blacklist / 2 surnames / 3 given_names / 4 genre-tropes / 5 meta-creativity-rules / 6 anti-trope-seeds / 7 题材 anti-trope-*，后续 US-003/004/005/006 继续递增追加。
---

## 2026-04-17 - US-004
- 新建 `ink-writer/skills/ink-init/references/creativity/golden-finger-rules.md`（1104 汉字）。
- 内容：GF-1 非战力维度 8 类（信息/时间/情感/社交/认知/概率/感知/规则）+ 禁用维度；GF-2 代价可视化 10 合格示例 + 校验伪代码；GF-3 一句话爆点 10 组合格/不合格对比；金手指禁止词列表 22 条（修为暴涨/无限金币/系统签到/作弊器/外挂/觉醒面板/熟练度+1 等）；校验算法 5 次重抽 + 降档逻辑（激进→平衡→保守）；`gf_checks` 和 `gf_downgrade_log` 两字段定义。
- 修改 `ink-writer/skills/ink-init/SKILL.md`：Quick Step 0 第 8 条加载 golden-finger-rules.md（L1 必读），原第 8 条题材 anti-trope-* 顺延为 9；Quick Step 2 之前插入 Quick Step 1.5 段落（三项校验 / 通过条件 sum≥2 / 重抽与降档 / 输出产物）。
- Files changed: 2（新增 golden-finger-rules.md；编辑 SKILL.md）。
- **Learnings for future iterations:**
  - Quick Step 0 加载列表当前编号：1 blacklist / 2 surnames / 3 given_names / 4 genre-tropes / 5 meta-creativity-rules / 6 anti-trope-seeds / 7 perturbation-engine / 8 golden-finger-rules / 9 题材 anti-trope-*。
  - Quick Step 1.5 输出字段 `gf_checks`（3 元 0/1 数组）与 `gf_downgrade_log`（数组）—— US-008 创意指纹「金手指维度」字段直接消费 gf_checks；US-010 档位降档逻辑应复用本文降档算法，避免双写。
  - 降档规则：激进→平衡→保守，保守档不再降；US-009 档位系统（1/2/3/4 含疯批）需在实现时扩展本逻辑把档位 4/3/2/1 映射一致。
  - PRD US-004 验收没有字数上下限，但文件内 4 个代码块（伪代码）+ 10 对比表 + 禁止词列表挤到 1104 汉字，控制得当。
---

## 2026-04-17 - US-005
- 新建 `ink-writer/skills/ink-init/references/creativity/style-voice-levels.md`（2497 汉字）。
- 内容：V1 文学狂野 / V2 烟火接地气 / V3 江湖野气三档定义（人名/书名/句式样例各套）；L0-L3 四级敏感词分类 + 示例；档位×密度矩阵（档位 1 = 0% / 档位 2 ≈ 0.2% / 档位 3 = 0.5%-0.8% / 档位 4 = 0.8%-1.5%，L2 仅 V3）；Quick Step 1.6 分配算法（stable_hash 洗牌 + 档位 1 V3→V1 回退 + 档位 4 至少一套 V3）；题材×档位适配星表；V1/V2/V3 各 8 条句式落地库；常见跑偏修复表；Voice 一致性约束；与 polish-agent 去 AI 味规则衔接。
- 修改 `ink-writer/skills/ink-init/SKILL.md`：Quick Step 0 加载列表第 9 条加入 style-voice-levels.md（L1 必读），原第 9 条题材 anti-trope-* 顺延为第 10 条；Quick Step 1.5 之后插入 Quick Step 1.6「语言风格三档分配」段落（算法/密度控制/输出字段 style_voice+style_voice_name+density_target+sample_line+vocabulary_allowlist）。
- Files changed: 2（新增 style-voice-levels.md；编辑 SKILL.md）。
- **Learnings for future iterations:**
  - Quick Step 0 加载列表当前编号：1 blacklist / 2 surnames / 3 given_names / 4 genre-tropes / 5 meta-creativity-rules / 6 anti-trope-seeds / 7 perturbation-engine / 8 golden-finger-rules / 9 style-voice-levels / 10 题材 anti-trope-*。
  - US-006（书名/人名算法升级）给 given_names.json 新增的 `rough/smoky/jianghu` 风格标签需与本文档 V1/V2/V3 档位映射：rough→V1, smoky→V2, jianghu→V3；blacklist 扩展词（神帝/龙傲天 等）在本文档 §十常见跑偏修复表已声明由 blacklist.json 拦截，US-006 实现时直接新增条目即可。
  - US-008 创意指纹「语言档位」字段消费本文档 `style_voice + sample_line`；US-009 档位询问需把 4 档映射到本文档密度矩阵；US-010 3 套方案差异化维度包含 style_voice，本 Step 1.6 硬约束已覆盖「两两不同档」。
  - 档位 1 保守遇 V3 时回退到 V1（而非 V2），保留最大风格差异；日志字段 `style_fallback` 命名已固定。
  - 字数校验口径与 US-001/003 一致：`python3 -c "import re; print(len(re.findall(r'[\u4e00-\u9fff]', open(...).read())))"`。
---

## 2026-04-17 - US-006
- 扩展 `data/naming/given_names.json`：新增 `rough/smoky/jianghu` 三个风格桶（每桶 male=34 / female=30，共 192 条新素材）。桶内新增 `_style_tag` 与 `_description` 元字段便于下游识别，`_style_tag` 直接映射 style-voice-levels.md 的 V1/V2/V3。
- 新建 `data/naming/nicknames.json`（110 条江湖绰号）：每条含 `rarity(1-5)` 与 `style_tags`（jianghu/rough/smoky/hei_dao/wu_lin 可多选）；稀有度 4+5 合计 ≥35%，覆盖生理缺陷型（独眼/瞎/哑/瘸/癞）+ 性格型（臭嘴/闷瓜/烂命）+ 江湖绰号型（刀疤/夜行乌/穿心钉）。
- 新建 `data/naming/book-title-patterns.json`（V1=54 / V2=57 / V3=59 共 170 条模板）：7 修辞标签覆盖计数 pun=13 / homophone=11 / antithesis=13 / irony=81 / oxymoron=23 / concrete_abstract=45 / anachronism=56，全部 ≥10 条硬门槛通过；文末附 `rhetoric_tag_coverage_audit` 供测试复用。
- 扩展 `data/naming/blacklist.json`：新增 `book_title_suffix_ban.tokens` 19 条（神帝/至尊/无敌/主宰/龙傲天/战神/剑神/丹帝/仙尊/魔尊/武帝/霸主 等）、`book_title_prefix_ban.tokens` 14 条（我的/全球/最强/重生成/穿越成/校花的 等）、`name_combo_ban`（7 姓 × 8 名末字笛卡儿积禁用：萧/林/叶/楚/顾/秦/陆 × 尘/风/寒/云/逸/辰/墨/天）。
- 修改 `ink-writer/skills/ink-init/SKILL.md`：Quick Step 0 加载列表第 10/11 条新增 nicknames.json + book-title-patterns.json（L1 必读），题材 anti-trope-* 顺延第 12 条；Quick Step 1.6 之后插入 Quick Step 1.7「书名与人名校验」段落（书名抽取+黑名单+修辞标签标注 + 人名风格桶映射 V1→rough/V2→smoky/V3→jianghu + 笛卡儿积组合禁用校验 + 输出字段 title_rhetoric_tags/nickname?/name_checks/name_retry_log）。
- Files changed: 5（1 新增 nicknames.json + 1 新增 book-title-patterns.json + 3 编辑 given_names/blacklist/SKILL.md）。
- **Learnings for future iterations:**
  - Quick Step 0 加载列表当前编号：1 blacklist / 2 surnames / 3 given_names / 4 genre-tropes / 5 meta-creativity-rules / 6 anti-trope-seeds / 7 perturbation-engine / 8 golden-finger-rules / 9 style-voice-levels / 10 nicknames / 11 book-title-patterns / 12 题材 anti-trope-*。
  - `given_names.json` 在已有 classical/modern/martial/scholarly/cold 五档基础上保持扁平结构新增 rough/smoky/jianghu；桶内引入 `_` 前缀元字段（`_style_tag / _description`）作为非数据注释，下游消费者遍历 male/female 时只取非下划线 key。
  - `book-title-patterns.json` 的 `rhetoric_tag_coverage_audit` 字段只是清单样本，真正的硬门槛校验应用 `Counter(tag)>=10` 统计（US-014 测试直接消费）。
  - `blacklist.json` 新增三段结构：`name_combo_ban` / `book_title_suffix_ban` / `book_title_prefix_ban`；现有 male/female 纯字符串数组兼容未动，Quick Step 1 原校验逻辑无需改动，Quick Step 1.7 单独消费新字段。
  - `given_names.json` 风格桶 → style_voice 档位映射：rough=V1 / smoky=V2 / jianghu=V3；档位 4 疯批可混抽 V3+V1（见 style-voice-levels.md §算法），此时 `given_names` 桶选取由 style_voice 二次抽签决定，不依赖原方案题材。
  - 已验证 Python JSON 解析无错：`python3 -c "import json; [json.load(open(f)) for f in (...)]"`；US-014 自动化测试可直接用 `json.load + Counter`。
---

## 2026-04-17 - US-003
- 新建 `ink-writer/skills/ink-init/references/creativity/perturbation-engine.md`（1579 汉字）。
- 内容：扰动对定义（跨 category + 稀有度门槛）/ 档位 N 矩阵（1/2/3/5）/ 5 种模式 A-E（时代错位 / 职业反差 / 尺度错位 / 规则颠倒 / 感官替换，每种 3 示例）/ Python 伪代码（stable_hash(timestamp+genre+draft_index) 固定 RNG）/ 3 套方案整对去重 / `perturbation_pairs` schema 字段含 injection_note / 与 Layer 1/2 协作。
- 修改 `ink-writer/skills/ink-init/SKILL.md` Quick Step 0 加载列表新增第 7 条 perturbation-engine.md（L1 必读），原 7 题材 anti-trope-* 顺延为 8。
- Files changed: 2（新增 perturbation-engine.md；编辑 SKILL.md）。
- **Learnings for future iterations:**
  - 扰动对 schema 字段约定为 `perturbation_pairs`（数组，每对含 pair_id/pattern/seed_a/seed_b/injection_note）；US-008 创意指纹「使用扰动对」字段直接消费此字段。
  - 档位 N 矩阵在本文件第 2 章定下：1/2/3/5。后续 US-009/010 档位实现需保持一致，不要重复定义。
  - 抽取算法以 stable_hash 固定种子，方便 US-014 写测试时构造可复现 fixture。
  - Quick Step 0 加载列表当前编号：1 blacklist / 2 surnames / 3 given_names / 4 genre-tropes / 5 meta-creativity-rules / 6 anti-trope-seeds / 7 perturbation-engine / 8 题材 anti-trope-*。
---

## 2026-04-17 - US-007
- 新建 `data/market-trends/README.md`（目录用途 + 固定 2 检索源 + 4 条硬编码检索语 + 缓存命名规则 cache-YYYYMMDD.md + Markdown 缓存格式（两平台 Top 10 + 5-8 热门套路关键词 + 共通套路列表）+ 90 天保留 + fallback 规则（7 天内替代 / 7 天外降级 fetch_status:none）+ 消费者清单）。
- 修改 `ink-writer/skills/ink-init/SKILL.md` Quick Step 0 末尾（第 39 行后）插入「WebSearch 子步骤：双平台榜单反向建模」段落：4 条硬编码检索语、两平台并行执行、15s 超时、当日缓存命中跳过、fallback 最近 7 天缓存、fetch_status 三态、`market_avoid` 字段供 Quick Step 1 规避 Top 5 共通套路。
- `allowed-tools` frontmatter 已含 `WebSearch WebFetch`（无需改动）。
- Files changed: 2 新增（data/market-trends/README.md）+ 1 编辑（SKILL.md）。
- **Learnings for future iterations:**
  - data 顶层已有 `cultural_lexicon / editor-wisdom / hook_patterns.json / naming`，本次新增 `market-trends/` 与 `naming/` 平级，不污染 skills/ 目录。
  - 缓存文件名用 UTC+8 日期 `cache-$(date +%Y%m%d).md`；Quick Step 0 的 Read 需用 `${CLAUDE_PLUGIN_ROOT}/../../data/market-trends/cache-$(date +%Y%m%d).md` 相对路径。
  - US-008 创意指纹「反向规避」字段直接消费 Quick Step 1 每套方案的 `market_avoid` 数组；fetch_status=none 时填「无当日数据」。
  - fetch_status 三态：ok（当日命中）/ fallback（7 天内近似）/ none（7 天内无缓存），前端提示语与方案硬约束都由此分支。
  - WebSearch 并行 4 条检索语聚合落盘的动作由 Quick 模式首次运行时触发；Deep 模式不强制（本 PRD 未要求）。
---

## 2026-04-17 - US-008
- 修改 `ink-writer/skills/ink-init/SKILL.md`：Quick Step 1 输出模板尾部追加 `#### 🧬 创意指纹` 板块示例（5 字段 Markdown 列表 + ≤200 字提示 + 3 套差异性提示行）；Quick Step 2 开头插入「🧬 创意指纹板块规格」小节（表格定义 5 字段的数据源 meta_rules_hit / perturbation_pairs / gf_checks / style_voice+sample_line / market_avoid，档位下限 1→≥1/2→≥2/3→≥3/4→≥5 元规则；2 对扰动起步），并追加差异性硬约束（元规则集合两两无交集或 ≤1 + 扰动对两两不重叠 + 语言档位两两不同）+ 违反退回 Quick Step 1 重抽。
- Files changed: 1（编辑 SKILL.md）。SKILL.md 963 行（US-011 上限 900 行将在后续用 Step 2 模板精简补救）。
- **Learnings for future iterations:**
  - 创意指纹板块的 5 字段数据源与上游的一一映射已锁死：meta_rules_hit(US-001) / perturbation_pairs(US-003) / gf_checks(US-004) / style_voice+sample_line(US-005) / market_avoid(US-007)；US-009/010 若改档位矩阵，只需同步更新指纹的「档位下限」行而不动字段清单。
  - 创意指纹位置放在每套方案字段区内最后（`#### 🧬 创意指纹`），而非 Quick Step 2 输出时再生成——这样用户看方案列表即含指纹，减少 Quick Step 2 后处理的模板分叉。
  - 差异性硬约束的第三项「语言档位」可复用 Quick Step 1.6 已硬约束的 V1/V2/V3 各占一档，不需额外校验逻辑；真正需要 Step 1 自检的只有前两项（元规则集合 + 扰动对）。
  - US-011 「SKILL.md ≤900 行」约束下，US-008 新增约 28 行；后续 US-009/010/011 若继续新增段落，需考虑把 Deep Mode 与 Quick Mode 的共享模板抽到 reference 文件以压缩主文。
---
