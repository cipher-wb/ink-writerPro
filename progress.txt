# Ralph Progress Log
Started: 2026年 4月17日 星期五 22时40分00秒 CST
Feature: v13 Health Audit Fixes (Step 2)
Source: docs/engineering-review-report-v5.md + tasks/prd-v13-step2-fixes.md

## 🛑 零回归原则（MUST follow every US）
**用户硬约束**：所有修复不得破坏原有写作能力，只能正向优化，产出软件不可以比原来的差。

**每个 US 执行步骤**：
1. **Baseline**：运行 `pytest --no-cov` 记录通过/失败数
2. **Implement**：按 US acceptance criteria 改代码
3. **Verify**：再跑 `pytest --no-cov` 对比 baseline，无新增失败才算 pass
4. **Regression 豁免**：若某测试本身需因 US 行为变更而调整，在 `notes` 字段明示测试名 + 调整理由 + 新旧预期对比
5. **写作链路保护**：涉及 context/writer/review/polish 主链路的改动，必须手动跑一次 `/ink-write` 观察输出不崩

## Context
- Step 1 深度健康审计已完成（12 份子报告 + 1 份总报告）
- 总报告位置：`docs/engineering-review-report-v5.md`
- 健康度总评 54/100，3 Blocker + 8 Critical + 9 Major
- 架构决策已就位：
  - FIX-02 → 方案 B（诚实降级，US-023）
  - FIX-03 → 方案 A（SQL-first 修代码，US-024 + US-025）
  - FIX-04 → 设计稿 `tasks/design-fix-04-step3-gate-orchestrator.md`（影子模式，本 PRD 中仅前置 US-005）

## Excluded（下轮处理，人工决策）
- FIX-11 双 Python 包合并（XL 重构 5-10d）
- FIX-16 真实 100 章压测（需 token 预算）
- FIX-17 AutoNovel 反向传播借鉴（XL 新模块）
- FIX-18 Novelcrafter Progressions 追踪（L 新模块）
- FIX-04 Phase A/B/C 完整实施（仅前置 US-005 改读 index.db 在本 PRD）

## Codebase Patterns
- Python 源码主目录：`ink_writer/`（下划线，v11+ 新模块）
- 数据层/业务脚本：`ink-writer/scripts/data_modules/`（横杠下划线，v5-v9 核心）
- Agent 规格目录：`ink-writer/agents/`（US-402 已消除双目录）
- Skill 目录：`ink-writer/skills/`
- CLI 入口：`ink-writer/scripts/ink.py`
- pytest 配置：`pytest.ini`（--cov-fail-under=70；US-004 降到 30）
- 依赖：`requirements.txt`（顶层）
- 核心数据文件：`state.json`（项目级）+ `index.db`（SQLite, 30+ 表）
- 测试：`tests/` 当前 2028 tests，baseline 跑 `pytest --no-cov -q` 记录

## Verification Principles
- "Typecheck passes" ≈ `python3 -c 'import <module>'` 成功 + pytest 子集通过
- Markdown = 文件可读、frontmatter 合法、相对链接有效
- JSON = `python3 -c 'import json; json.load(open(...))'`
- 数据层改动 → 用 `:memory:` SQLite migration 烟测
- CLI 改动 → 实跑一次观察

## US 依赖链（Ralph 自动按 priority 顺序跑，但存在隐含依赖）
- US-009 → US-010（schema 先扩字段）
- US-024 → US-025（flush 顺序先定，再扫尾直写点）
- US-020/021 → US-022（先补文档数字，再加校验脚本）
- US-008 Style RAG → 独立，但需 US-001 的 faiss-cpu 依赖就位

---

## 2026-04-17 22:58 - US-001
- 修改 `requirements.txt`：新增 6 个 ink_writer/ 核心依赖（~=X.Y 兼容版本约束）
  - numpy~=2.4 / faiss-cpu~=1.13 / PyYAML~=6.0 / jsonschema~=4.26 / sentence-transformers~=5.4 / anthropic~=0.89
- 实测版本基于当前系统 Python 3.14 已安装版本
- Verification: 6 imports OK; pytest --collect-only 2028 tests; full regression 2028 passed（对比 baseline 零回归）
- Files changed: 1
- Commit: 8322094
- **Learnings for future iterations:**
  - `ralph/` 目录在 `.gitignore` 中，prd.json 不随提交走，使用 `git add -f` 或直接编辑后 commit（但本次无需）
  - pytest 9.x 的 `--no-cov -q` 输出被 tee 截断，summary 行常丢；可用 `> /tmp/x.txt; grep -E "passed|failed"` 更稳
  - pytest.ini 的 `addopts = --cov-fail-under=70` 即使加 `--no-cov` 也会被 load，但会跳过门禁检查
  - 系统 Python 路径：/Library/Frameworks/Python.framework/Versions/3.14/bin/python3；无 pip 命令（系统未装），但依赖都已手动安装
  - Regression 验证全流程 ~2 分钟（pytest --no-cov），每 US 执行都跑一次
---

## 2026-04-17 23:02 - US-002
- 修改 `pyproject.toml`：新增 [build-system] + [project] + [project.urls] + [tool.setuptools.packages.find]
  - name=ink-writer / version=13.8.0（对齐 plugin.json）/ license=GPL-3.0
  - dependencies 13 条（聚合 US-001 的 6 条 + scripts/dashboard 已有的 7 条）
  - packages.find 显式只包 `ink_writer/`，排除 tests / benchmark / archive / docs 等辅助目录
- 跳过 [project.scripts]：`ink_writer/cli.py` 不存在（AC 允许跳过）
- Verification: 干净 venv `pip install -e .` 成功；`pip show ink-writer` 返回 Version 13.8.0；pytest 2028 passed 零回归
- Files changed: 1
- **Learnings for future iterations:**
  - 系统 pip 不存在但 `python3 -m venv` + `venv/bin/pip` 可用；测试隔离安装用 `/tmp/venv-xxx`
  - `[tool.setuptools.packages.find]` 的 include/exclude 对大目录项目必需，否则 setuptools 会把 archive/ / benchmark/ 等非包目录也打包
  - pyproject.toml TOML 语法严格，`list { text = ... }` 这样的内联表要注意花括号
  - 13 deps 里重复的（如 anthropic 同时在 requirements.txt 和 pyproject.toml）是预期的，pip 会 dedup
---

## 2026-04-17 23:07 - US-003
- 修改 `.github/workflows/ci-test.yml`：两 install steps 追加 `pip install -r requirements.txt`；新增 "Verify ink_writer core imports" step
- Smoke test 覆盖：editor_wisdom / style_rag / semantic_recall / parallel / checker_pipeline 5 子模块 + Retriever import-only（不实例化避免 30s BAAI 加载）
- Verification: YAML 合法（yaml.safe_load）；干净 venv 模拟 CI 通过；pytest 2028 passed 零回归
- Files changed: 1
- **Learnings for future iterations:**
  - `Retriever` 类可以 import-only 不触发模型加载（~30s 代价），只在第一次调用 `query()` 时才 lazy-load（待 US-006 单例化后确认）
  - CI workflow trigger paths 已包含 `requirements.txt` 变更触发，无需额外配置
  - 本地模拟 CI：`python3 -m venv /tmp/ci-check && ./ci-check/bin/pip install -r requirements.txt` 是每次改依赖后必跑的快速验证
  - **P0 三件套（Blocker）US-001~003 全部完成**：依赖声明 + pyproject + CI 安装，干净 runner 上首次可正确 install
---

## 2026-04-17 23:10 - US-004
- 修改 `pytest.ini`：`--cov-fail-under=70` → `30`；增加多行注释说明历史+现状+未来计划
- **意外发现**：实测综合覆盖率 **82.05%**（v5 审计引用的 13.62% 是 `scripts/` 子集，不是全量）。所以原 70% 门禁其实是过的；但按 PRD AC 和用户确认仍降到 30 保安全余量
- Verification: pytest --no-cov 2028 passed 零回归；pytest 带 cov 显示 "Required test coverage of 30% reached. Total coverage: 82.05%"
- Files changed: 1
- **Learnings for future iterations:**
  - `.coveragerc` 的 `[run] source` 决定 pytest-cov 统计范围，不是整个仓库
  - v5 审计 R2（High）项实际不是 Blocker 级别问题（综合 82% vs 70% 门禁是过的），审计视角偏狭窄导致误报
  - 后续 US-010 补测试计划可以把门禁拉回 70 或更高（建议 80 匹配当前）
  - **此经验适用**：v5 审计的其它发现也可能是子集视角；执行每个 US 前值得先实测验证原问题仍存在
---

## 2026-04-17 23:22 - US-005
- 修改 `ink-writer/scripts/step3_harness_gate.py`：新增 _load_review_data_from_index_db 和 _load_review_data_from_json helper；check_review_gate 改用新数据源顺序（index.db→JSON fallback→两者皆无则 FAIL）
- 新建 `tests/harness/test_step3_harness_gate.py`：6 项测试覆盖 index.db pass/fail、legacy JSON warning、minimal payload 降级
- 修改 `pytest.ini`：testpaths 追加 tests/harness
- Verification: 2034 passed 零回归（+6 新测试）
- **Learnings for future iterations:**
  - review_metrics 表已存在（index_manager.py 创建），有 save_review_metrics / get_recent_review_metrics 方法；但也可以直接 sqlite3 读（测试独立不依赖 IndexManager）
  - review_payload_json 列有时为 NULL（老迁移），此时只能基于列组（overall_score/severity_counts）做 minimal gate 判断，checker_results 黄金三章/reader-simulator 分支跳过
  - v5 审计说"100% 静默 PASS"具体 mechanism：reports_dir 存在但无匹配 JSON → 老代码 return result(默认 pass)。修复后明确 FAIL
  - action 字段扩展 "rerun_step3_review"（原只有 rewrite_step2a / continue），下游 harness 处理时需识别
---

## 2026-04-17 23:32 - US-006
- 修改 `ink_writer/editor_wisdom/retriever.py`：新增 module-level `_RETRIEVER_CACHE` 字典 + `get_retriever(index_dir)` + `clear_retriever_cache()`
- 3 处生产路径改用 `get_retriever()`：writer_injection.py:63 / context_injection.py:71 / step3_harness_gate.py:209
- 新建 `tests/harness/test_retriever_singleton.py`（3 测试，pytest.importorskip 优雅处理 faiss 不可用）
- Verification: 2037 passed 零回归
- **Learnings for future iterations:**
  - **重要发现**：tests/editor_wisdom 不在 pytest.ini testpaths 里，该目录的测试长期未跑；尝试加入时暴露 4 failed + 6 error 预存在问题。未修复，待 US-010 处理
  - 新增单例测试应放 `tests/harness/`（已在 testpaths）或需要同时修 testpaths 漏配
  - Retriever 单例由 `dict[str, Retriever]` 按 index_dir key 缓存；不同 project 切换 index 时新增实例，同 project 共用
  - 系统其它疑似"孤儿测试目录"可 `grep -c tests/editor_wisdom pytest.ini` 验证
---

## 2026-04-17 23:38 - US-007
- 修改 `scripts/editor-wisdom/03_classify.py` + `scripts/editor-wisdom/05_extract_rules.py`
  - main() 入口 `os.getenv("ANTHROPIC_API_KEY")` 校验，缺失 sys.exit
  - 循环内 consecutive_failures 计数，≥5 连续失败 sys.exit 保护 API 额度
- 新建 `tests/harness/test_api_key_guard.py`（subprocess 参数化测试 2 项）
- Verification: 2039 passed 零回归（+2 新测试）
- **Learnings for future iterations:**
  - 测试脚本入口校验用 subprocess（pytest 直接 import 会 capture 太多 side effects）；env 过滤时显式从 os.environ 复制并删 key
  - MAX_CONSECUTIVE_FAILURES = 5 是经验值，足够吞 transient rate limit，同时保护 cost
  - 成功一次即 reset counter 是关键（避免 intermittent 错误被误判为永久失败）
---

## 2026-04-17 23:52 - US-008
- 修改 `ink_writer/style_rag/retriever.py`：__init__ 加 auto_build + db_path 参数；3 档降级（FAISS→subprocess 构建→SQLite 直查→raise）；新增 _try_auto_build() / _sqlite_retrieve() / _row_to_fragment() 方法
- 修改 `ink-writer/scripts/init_project.py`：main() 末加 _try_ensure_style_rag_index()
- 新建 `tests/harness/test_style_rag_fallback.py`（3 测试）
- 调整 `tests/style_rag/test_style_rag.py`（2 测试）：新行为下要 raise 需 auto_build=False + nonexistent db_path（AC 允许）
- Verification: 2042 passed 零回归（+3 新 -2 改）
- **Learnings for future iterations:**
  - **重要经验**：改变 constructor 的行为（如默认降级而非 raise）必然影响既有测试；需同步调整；用户硬约束"零回归"解读为"测试改动需有明确理由+预期变更明示"，而非"测试不能动"
  - subprocess 调用需 timeout（本次 600s）避免挂死
  - SQLite fallback 的 score 归一化：`quality_score / 100.0` 近似语义相似度
  - `random.sample(rows, min(k, len(rows)))` 避免固定排序导致每次同结果
  - init_project.py 的 Style RAG 自动构建首次 ~3min（用户体验 trade-off，但一劳永逸）
---

## 2026-04-17 23:59 - US-009
- 修改 `ink-writer/scripts/state_schema.py`：新增 `CreativeFingerprint` Pydantic 模型；ProjectInfo 加 creative_fingerprint 字段；schema_version 9→10
- 新建 `tests/harness/test_creative_fingerprint_schema.py`（5 测试）
- 调整 `ink-writer/scripts/data_modules/tests/test_state_schema.py`：5 处 schema_version assertion 9→10；1 处保留 9（显式 input 9 的不升级）
- Verification: 2047 passed 零回归（+5 新，5 处改）
- **Learnings for future iterations:**
  - Pydantic BaseModel 不会"自动升级" schema_version；若需升级逻辑需在 model_validate 后手动 sm.schema_version = X
  - ProjectInfo 结构随 schema 演进加字段时，旧数据自动兼容（因 extra="allow"）
  - `List[Dict[str, Any]]` 适合承载 perturbation_pairs 这种半结构化数据
  - sed 批量替换要小心 false-positive：测试内显式 input schema_version=9 的不应被改
  - **下一 US（US-010）依赖本 US**：init_project.py 消费 Quick 输出写入这 5 字段
---

## 2026-04-18 00:04 - US-010
- 修改 `ink-writer/scripts/init_project.py`：init_project 签名加 5 参数；_coerce_list helper；写入 state["project_info"]["creative_fingerprint"]；main() CLI 加 5 个 --meta-rules-hit 等参数
- 新建 `tests/harness/test_init_creative_fingerprint.py`（5 测试）
- Verification: 2052 passed 零回归（+5 新）
- **Learnings for future iterations:**
  - _coerce_list helper 设计：JSON → 逗号分隔 → 空列表三档降级，适合 CLI + SKILL.md 调用双入口
  - `str | list` type union 可同时兼容 SKILL.md 的 JSON 字符串和编程式 list 入参
  - Quick 模式 SKILL.md 后续会生成 `--meta-rules-hit '["M01","M03"]'` 这种 CLI 调用串
---

## 2026-04-18 00:10 - US-011
- 重写 `ink-writer/skills/ink-resolve/SKILL.md`：Step 1 改读 SQL disambiguation_log 表；Step 4 改用 `sql_state_manager.resolve_disambiguation_entry(entry_id)` + `idx.rebuild_state_dict()`；新增迁移说明 + 禁止项明示
- 新建 `tests/harness/test_resolve_sql_path.py`（2 测试：语法扫描 pass / API 集成 skip）
- Verification: 2053 passed + 1 skipped 零回归
- **Learnings for future iterations:**
  - **pytest context 下 IndexManager 初始化坑**：直跑 python3 能正常 `IndexManager(cfg)`，但 pytest 下 `self.config` 被 mixin 覆盖成 IndexManager 自身（待查，疑似 pytest-asyncio 或 fixture interaction）。绕过方案：集成测试放 ink-writer/scripts/data_modules/tests/ 里跑，那里 fixture 路径正确
  - SQL 数据源：`disambiguation_log` 表有 category/status/payload/chapter 字段，`category='pending'` 是 active 列表
  - `sql_state_manager.resolve_disambiguation_entry(id)` UPDATE status='resolved' + resolved_at=now()
  - rebuild_state_dict() 是 SQL→JSON 视图重生的入口，v13 US-011 明确禁止跳过它直写 state.json
---

## 2026-04-18 00:14 - US-012
- 修改 `ink_writer/editor_wisdom/llm_backend.py`：call_llm 加 timeout + max_retries 参数；SDK + CLI 路径都显式 timeout；APITimeoutError / TimeoutExpired 统一转 TimeoutError；linear backoff 重试
- 新建 `tests/harness/test_llm_timeout.py`（3 测试）
- Verification: 2056 passed 零回归（+3 新）
- **Learnings for future iterations:**
  - anthropic SDK 超时用 `anthropic.Anthropic(timeout=60)` 传给 httpx；异常名含 "Timeout" 子串即判定为超时类
  - CLI 路径超时 `max(timeout, 90)` 保底因为 `claude -p` 启动慢
  - linear backoff：`time.sleep(LLM_BACKOFF_BASE_S * (attempt+1))` 避免立即重试打到同一 rate limit
  - test 中 `monkeypatch.setattr(time, "sleep", lambda _: None)` 可加速带 sleep 的测试
---

## 2026-04-18 00:19 - US-013
- 修改 `ink_writer/parallel/pipeline_manager.py`：PipelineConfig 加 chapter_timeout_s（默认 1800，env 覆盖）；ChapterStatus 加 TIMEOUT_FAILED；run() 章 task 包 asyncio.wait_for；gather 后判 TimeoutError 标 TIMEOUT_FAILED
- 新建 `tests/harness/test_pipeline_chapter_timeout.py`（4 测试）
- Verification: 2060 passed 零回归（+4 新）
- **Learnings for future iterations:**
  - `asyncio.gather(return_exceptions=True)` 返回列表中 TimeoutError 是 `asyncio.TimeoutError`（Python 3.10+ 等价于 builtin TimeoutError）
  - importlib.reload 在多测试间共享枚举会导致 `MyEnum.VAL != MyEnum.VAL`（identity mismatch），慎用
  - `chapter_timeout_s` 作为 dataclass field default 读 env 只在 module 首次 import 时固化；动态 env 切换用显式传参
---
