# Ralph Progress — deep-review-and-perfection

## Codebase Patterns
- thread-lifecycle-tracker.md unifies foreshadow + plotline tracking; old specs kept for backward compat
- checker-output-schema.md must list ALL active checkers in both metrics sections and summary format
- shared-checker-preamble.md contains common input/output/scoring rules referenced by all checkers
- IndexManager auto-creates all DB tables on init; never create tables manually in fixtures — let IndexManager.__init__ handle schema, then INSERT test data
- E402 lint warnings are expected for scripts using sys.path manipulation before imports (project_locator, data_modules)
- DataModulesConfig.from_project_root(path) expects `.ink/` dir with `state.json` under project_root
- Review metrics are stored in review_metrics table with JSON-encoded dimension_scores, severity_counts, review_payload_json
- chapter_reading_power table stores per-chapter hook/micropayoff data; tool_call_stats has limited columns (no duration_ms/tokens_used)
- pytest.ini testpaths: `ink-writer/scripts/data_modules/tests tests/baseline tests/audit tests/hooks tests/pacing tests/emotion tests/style_rag tests/anti_detection tests/cultural tests/memory_arch`; pythonpath includes `scripts`
- Agent specs are markdown with YAML frontmatter (name/description/tools/model); IO sections labeled "输入"/"输出格式"
- Import cycle detection works via pure Python AST + DFS; no need for networkx dependency
- All 19 agent specs in single canonical directory `ink-writer/agents/` (US-402 eliminated dual-directory split)
- benchmark/corpus_index.json has 117 books; use `collections` field for ranking, `dir` field for filesystem lookup
- benchmark/config.py has SCENE_TYPE_KEYWORDS / EMOTION_KEYWORDS — reuse for text analysis instead of reinventing
- Hook types taxonomy (5 core): crisis, mystery, emotion, choice, desire — aligned with reader-pull-checker agent
- Position types: open (chapter opening), mid (mid-chapter tension), cliff (chapter-end cliffhanger)
- Genre hook templates in GENRE_HOOK_TEMPLATES cover 13 genres; each genre has 5 templates across different hook types
- data/hook_patterns.json is the canonical hook pattern library; regenerate via `python3 scripts/mine_hook_patterns.py`
- Chapter outline schema is Markdown-based (not JSON); defined in ink-plan/SKILL.md lines 448-468
- hook_contract field format: `类型=X | 兑现锚点=第M章 | 兑现摘要=Y` — parsed by ink-writer/scripts/hook_contract.py
- Outline backfill migration: `python3 scripts/patch_outline_hook_contract.py --project-root <path>` — backs up originals before patching
- migrate.py CURRENT_SCHEMA_VERSION=9; v7→v8 adds hook_contract_config, v8→v9 adds single-source-of-truth (state_kv, disambiguation_log, review_checkpoint_entries)
- state.json is a derived view cache (v13 architecture); SQLite index.db is the single source of truth
- SQLStateManager.rebuild_state_dict() regenerates state.json from SQLite; called automatically when state.json is missing
- state_kv table stores singleton state fields (project_info, progress, protagonist_state, world_settings, strand_tracker, configs)
- StateManager._sync_state_to_kv() syncs singleton fields to SQLite after every save_state()
- reader-pull hook retry gate: `ink_writer.reader_pull.hook_retry_gate.run_hook_gate()` — mirrors editor_wisdom/review_gate.py pattern
- fix_prompt_builder: `VIOLATION_FIX_TEMPLATES` maps violation IDs to Chinese fix instructions; `normalize_checker_output()` handles both old and new output formats
- config/reader-pull.yaml: score_threshold=70, golden_three_threshold=80, max_retries=2
- polish-agent accepts `hook_fix_prompt` field for targeted hook/追读力 repairs (Step 1.5 in execution flow)
- ink-write Step 3.6 is the hook retry gate; runs after Step 3.5 harness gate, before Step 4
- high_point_scheduler: `ink_writer.pacing.high_point_scheduler.schedule_high_point()` — proactive cool-point recipe per chapter
- Cool-point type taxonomy (6 types): face_slap, hidden_strength, level_up_kill, authority_challenge, villain_fail, sweet_surprise
- Intensity levels: minor (小爽点), combo (组合爽点), milestone (里程碑爽点) — from cool-points-guide.md
- config/high-point-scheduler.yaml: max_consecutive_no_hp=2, combo_window=5, milestone_window=12, climax_zone_start=0.85
- ink-plan Step 2.5 is high-point scheduling; Step 2.6 is cool-point execution script (was Step 2.5)
- Chapter outline template `爽点配方` field format: `类型=X | 强度=Y | 兑现窗口=Z章`
- Cultural lexicon: `ink_writer.cultural_lexicon` module; config at `config/cultural-lexicon.yaml`; data at `data/cultural_lexicon/{genre}.json`
- Lexicon entry types: idiom, slang, dialect, era_word, jargon — validated in tests
- context-agent Step 2.6 injects cultural lexicon; writer-agent has corresponding section for using it
- `sample_lexicon()` uses chapter_no + seed_offset for deterministic but chapter-varying selection
- Semantic recall module: `ink_writer.semantic_recall` (config, chapter_index, retriever); FAISS index at `.ink/chapter_index/`
- ChapterVectorIndex stores chapter_memory_cards as FAISS vectors (BAAI/bge-small-zh-v1.5); build via `scripts/build_chapter_index.py`
- SemanticChapterRetriever: 3-way recall = semantic Top-K ∪ entity-forced ∪ recent-N; config at `config/semantic-recall.yaml`
- extract_chapter_context.py `_search_semantic_recall()` is the priority path; falls back to legacy RAG/BM25 if index unavailable
- pytest.ini testpaths now includes `tests/semantic_recall`
- foreshadow-tracker agent scans every chapter; priority-aware overdue: P0(grace=5), P1(grace=10), P2(grace=20)
- ink-plan Step 2.4 calls `scan_foreshadows()` + `build_plan_injection()`; forced_payoffs as hard constraint
- Chapter outline `伏笔处置` field: 无 | 埋设:[id] | 推进:[id] | 兑现:[id]
- Dashboard `/api/plot-threads/heatmap` endpoint + ForeshadowHeatmap React component
- voice_fingerprint module: `ink_writer.voice_fingerprint` (config, fingerprint, ooc_gate, fix_prompt_builder)
- `voice_fingerprint_json` TEXT column in character_evolution_ledger (added via _ensure_column, not CREATE TABLE change)
- VoiceFingerprint fields: catchphrases, speech_habits, vocabulary_level, tone, dialect_markers, forbidden_expressions
- `save_fingerprint_to_db()` / `load_fingerprint_from_db()` / `load_all_fingerprints()` for bulk operations
- `merge_fingerprint()` does append-only merge respecting max_* limits from LearningConfig
- `score_chapter_voice()` checks forbidden expressions (critical), catchphrase absence (medium), vocab mismatch (medium), distinctiveness (medium)
- `run_voice_gate()` pattern mirrors `run_emotion_gate()`: checker_fn → score → polish_fn retry → voice_blocked.md on fail
- migrate.py CURRENT_SCHEMA_VERSION=10; v9→v10 adds voice_fingerprint_config
- ink-write Step 3.9 is the voice fingerprint gate; runs after Step 3.8 anti-detection gate
- polish-agent Step 1.8 handles voice_fix_prompt (voice fingerprint repair)
- config/voice-fingerprint.yaml: score_threshold=60, max_retries=2, catchphrase_absence_chapters=3
- pytest.ini testpaths now includes `tests/voice_fingerprint`
- plotline-tracker agent: scans every chapter for main/sub/dark plotline inactivity
- Inactivity thresholds: main=3章, sub=8章, dark=15章; severity: main=critical, sub=high, dark=medium
- ink-plan Step 2.7 calls `scan_plotlines()` + `build_plan_injection()`; forced_advances as hard constraint
- Chapter outline `明暗线推进` field: [plotline_id]:推进描述, ... | 无
- ink-write Step 3.10 is the plotline gate; runs after Step 3.9 voice fingerprint gate
- Plotline data stored in `plot_thread_registry` with `thread_type='plotline'`; line_type in payload_json
- config/plotline-lifecycle.yaml: main_max_gap=3, sub_max_gap=8, dark_max_gap=15, max_forced_advances=2
- Dashboard `/api/plotlines` + `/api/plotlines/heatmap` endpoints for plotline visualization
- migrate.py CURRENT_SCHEMA_VERSION=11; v10→v11 adds plotline_lifecycle_config
- state_schema.py PlotThreads model has plotline_registry field
- pytest.ini testpaths now includes `tests/plotline`
- Prompt templates in `ink-writer/templates/prompts/` with `{{PROMPT_TEMPLATE:filename.md}}` reference pattern
- `_manifest.json` tracks template name, version, consumers, changelog
- `scripts/ab_prompts.py` commands: list, diff-all, show, compare
- audit_architecture.py `_strip_template_refs()` excludes `{{PROMPT_TEMPLATE:...}}` from n-gram analysis
- pytest.ini testpaths now includes `tests/prompts`
- Parallel pipeline: `ink_writer.parallel` module (PipelineManager + ChapterLockManager)
- ink-auto.sh supports `--parallel N` flag; delegates to Python asyncio orchestrator when N>1
- ChapterLockManager uses SQLite `parallel_locks.db` with TTL-based locks for cross-process coordination
- PipelineManager: batch-parallel model — N chapters per batch, checkpoints after each batch completes
- config/parallel-pipeline.yaml for parallel settings (max_parallel, cooldown, lock TTL)
- pytest.ini testpaths now includes `tests/parallel`; asyncio_mode=auto for async tests
- Prompt cache: `ink_writer.prompt_cache` module (config, segmenter, metrics)
- cache_control applied to editor-wisdom llm_backend.py and checker.py system prompts
- CacheMetricsTracker stores per-call cache hit/miss in `.ink/cache_metrics.db`
- config/prompt-cache.yaml: 7 stable segments (system_prompt, character_archive, worldbuilding, etc.) + 5 volatile
- pytest.ini testpaths now includes `tests/prompt_cache`
- Checker pipeline: `ink_writer.checker_pipeline` module (CheckerRunner + GateSpec)
- CheckerRunner: asyncio parallel with cancel_event for early termination on hard-gate failure
- Sync checkers wrapped in asyncio.to_thread(); async checkers run natively
- step-3-review-gate.md: full parallelism (removed max_concurrency=2 limit)
- pytest.ini testpaths now includes `tests/checker_pipeline`

---

## 2026-04-15 - US-001
- Implemented `scripts/measure_baseline.py`: end-to-end baseline measurement script
- Metrics: hook_density, strong_hook_ratio, avg_micropayoffs, avg_debt_balance, high_point_density, emotion_variance, avg_immersion, ai_taste_score, ooc_score, consistency_score, avg_chapter_seconds, avg_chapter_tokens
- Output: structured JSON to `benchmark/baseline_v12.json` with timestamp + git SHA
- Added `tests/baseline/test_measure_baseline.py` with 11 tests (schema, correctness, idempotency, empty project)
- Updated `pytest.ini` to include `tests/baseline` in testpaths and `scripts` in pythonpath
- Files changed: scripts/measure_baseline.py, tests/baseline/__init__.py, tests/baseline/test_measure_baseline.py, pytest.ini
- **Learnings for future iterations:**
  - tool_call_stats table does NOT have duration_ms or tokens_used columns — only tool_name, success, retry_count, error_code, error_message, chapter
  - Word counts are in chapters table; token estimates derived from word_count × 1.5
  - Performance timing must come from review_metrics created_at/updated_at delta, not tool_call_stats
  - The project has two agent directories: `ink-writer/agents/` (14 agents) and `agents/ink-writer/` (1 agent: editor-wisdom-checker)
---

## 2026-04-15 - US-002
- Implemented `scripts/build_reference_corpus.py`: selects Top-N books by collections, creates reference_corpus/ with manifest.json per book, analyzes text for hook/high-point metrics, outputs P25/P50/P75 percentiles
- Built 50-book reference corpus from existing 117-book scraped corpus
- `benchmark/reference_stats.json`: hook_density P75=0.2604, high_point_density P75=0.965
- Each book in `benchmark/reference_corpus/{title}/manifest.json` has: book_id, title, author, source, genre, chapters_count, license_note
- Chapter files are symlinked to avoid data duplication
- Added `benchmark/reference_corpus/` to .gitignore (copyrighted content)
- Added `tests/baseline/test_reference_corpus.py` with 23 tests (20 unit + 3 live corpus validation)
- Files changed: scripts/build_reference_corpus.py, tests/baseline/test_reference_corpus.py, benchmark/reference_stats.json, .gitignore
- **Learnings for future iterations:**
  - Existing corpus at benchmark/corpus/ has 117 books with metadata.json + chapters/ch00N.txt structure
  - benchmark/corpus_index.json is the master index with collections/genre/chapter_count per book
  - benchmark/config.py has SCENE_TYPE_KEYWORDS and EMOTION_KEYWORDS useful for text analysis
  - Hook density for real novels averages ~0.17 (end-of-chapter hooks are sparse but impactful)
  - High-point keyword density averages ~0.75 per 1000 chars across genres
  - compute_percentiles returns int for min/max when inputs are ints — use isinstance(x, (int, float)) in assertions
---

## 2026-04-15 - US-003
- Implemented `scripts/audit_architecture.py`: architecture static audit script
- Detects: import cycles (AST+DFS), unused modules, agent IO contract overlaps, repeated prompt fragments (n-gram dedup)
- Outputs `reports/architecture_audit.md` with summary, per-agent IO contract table, overlap analysis
- Results: 121 modules scanned, 1 import cycle (chapter_paths ↔ chapter_outline_loader), 29 unused module candidates, 15 agents with 8 overlap pairs, 29 repeated prompt fragments
- Added `tests/audit/test_audit_architecture.py` with 19 tests (fake fixture + live integration)
- Updated `pytest.ini` to include `tests/audit` in testpaths
- Files changed: scripts/audit_architecture.py, tests/audit/__init__.py, tests/audit/test_audit_architecture.py, pytest.ini, reports/architecture_audit.md
- **Learnings for future iterations:**
  - Pure Python AST parsing is sufficient for import cycle detection — no need for networkx
  - Agent spec files use YAML frontmatter with IO sections labeled "输入"/"输出格式" (Chinese)
  - Some agents (consistency-checker, continuity-checker, ooc-checker, pacing-checker) have identical descriptions → overlap detection catches them
  - When filtering test files by path, avoid `"test" in str(path)` — pytest tmp dirs contain "test" in directory names; use `path.name.startswith("test_")` instead
  - The editor-wisdom-checker in `agents/ink-writer/` is the only agent not in `ink-writer/agents/`
---

## 2026-04-15 - US-101
- Implemented `scripts/mine_hook_patterns.py`: mines hook patterns from reference corpus
- 208 unique patterns (≥200 requirement met): 60 crisis, 53 mystery, 35 emotion, 24 choice, 36 desire
- Positions: 162 cliff, 17 open, 29 mid
- 7847 raw matches across 1487 chapters from 50 books, deduplicated to 208 patterns
- 13 genre-specific template sets (玄幻/仙侠/都市/科幻/游戏/悬疑/历史/轻小说/末日/克苏鲁/电竞/直播文/系统流/穿越)
- Each pattern has: id, type, position, trigger_template, payoff_window_chapters, examples, source_refs
- Output: `data/hook_patterns.json`
- Added `tests/hooks/test_hook_patterns.py` with 29 tests (schema, correctness, deduplication, live corpus)
- Updated `pytest.ini` to include `tests/hooks` in testpaths
- Files changed: scripts/mine_hook_patterns.py, data/hook_patterns.json, tests/hooks/__init__.py, tests/hooks/test_hook_patterns.py, pytest.ini
- **Learnings for future iterations:**
  - Chinese punctuation (`"` / `"`) in Python regex strings causes syntax errors; use Unicode escapes (`\u201c`) or raw strings carefully
  - Fixture chapters must be ≥200 chars to pass the `len(text) < 200` filter in scanning functions
  - Genre templates add ~65 patterns (5 per genre × 13 genres), useful to guarantee hitting the 200-pattern minimum
  - Hook type taxonomy (crisis/mystery/emotion/choice/desire) is aligned with reader-pull-checker agent spec
  - Deduplication key is `position:hook_type:regex`; same regex at different positions counts as different patterns
---

## 2026-04-15 - US-102
- Implemented hook_contract enforcement in ink-plan chapter outline schema
- Added `钩子契约` field to chapter block template: `类型=X | 兑现锚点=第M章 | 兑现摘要=Y`
- ink-plan hard fail conditions now reject outlines missing hook_contract or with invalid type
- Created `ink-writer/scripts/hook_contract.py`: parsing, extraction, validation (5 valid types aligned with hook_patterns.json)
- Created `scripts/patch_outline_hook_contract.py`: backfill migration for existing outlines (guesses type from 钩子 field, defaults anchor to next chapter)
- Updated `ink-writer/skills/ink-migrate/SKILL.md`: added Phase 2.5 for outline hook contract backfill
- Updated `ink-writer/scripts/migrate.py`: v7→v8 migration adds hook_contract_config to state.json
- Added `tests/hooks/test_hook_contract_required.py` with 29 tests (parsing, validation, backfill, migration)
- Files changed: ink-writer/scripts/hook_contract.py, scripts/patch_outline_hook_contract.py, tests/hooks/test_hook_contract_required.py, ink-writer/scripts/migrate.py, ink-writer/skills/ink-plan/SKILL.md, ink-writer/skills/ink-migrate/SKILL.md
- **Learnings for future iterations:**
  - Chapter outline is pure Markdown with no JSON schema validation at load time — validation is prompt-level in ink-plan SKILL.md
  - The `钩子` field uses Chinese hook type names (悬念钩/危机钩/etc.) while hook_contract uses English types (mystery/crisis/etc.) — the mapping is in HOOK_KEYWORD_MAP
  - ink-migrate originally stated "不修改大纲文件" — US-102 adds a new phase specifically for outline migration, documented as Phase 2.5
  - migrate.py uses a decorator-based registration pattern: `@migration(from_version)` — each migration bumps schema_version
  - Outline sections are split by `### 第 N 章：` regex headers; bullet items use `- ` prefix
---

## 2026-04-15 - US-103
- Implemented `ink_writer/reader_pull/` module: config.py, fix_prompt_builder.py, hook_retry_gate.py
- `normalize_checker_output()`: normalizes reader-pull-checker output from {overall_score, hard_violations, soft_suggestions} to {score, violations[], fix_prompt}
- `build_fix_prompt()`: generates actionable Chinese fix instructions from violation list using VIOLATION_FIX_TEMPLATES
- `run_hook_gate()`: check→polish→recheck loop (max 2 retries), writes `chapters/{n}/hook_blocked.md` on failure
- Updated `reader-pull-checker.md`: added `fix_prompt` field to output schema with documentation
- Updated `polish-agent.md`: added `hook_fix_prompt` input field + Step 1.5 execution section
- Updated `ink-write/SKILL.md`: added Step 3.6 hook retry gate between Step 3.5 and Step 4
- Created `config/reader-pull.yaml`: score_threshold=70, golden_three_threshold=80, max_retries=2
- Added `tests/hooks/test_reader_pull_retry.py` with 31 tests (config, fix_prompt_builder, hook_retry_gate, integration, edge cases)
- All 1237 tests pass, coverage 82.54%
- Files changed: ink_writer/reader_pull/__init__.py, ink_writer/reader_pull/config.py, ink_writer/reader_pull/fix_prompt_builder.py, ink_writer/reader_pull/hook_retry_gate.py, config/reader-pull.yaml, ink-writer/agents/reader-pull-checker.md, ink-writer/agents/polish-agent.md, ink-writer/skills/ink-write/SKILL.md, tests/hooks/test_reader_pull_retry.py
- **Learnings for future iterations:**
  - The editor_wisdom/review_gate.py pattern (dataclass results, callable checker/polish, log+blocked file) is the canonical gate pattern — reuse it for new gates
  - polish_fn signature differs between gates: editor_wisdom uses (text, violations, chapter_no), reader_pull uses (text, fix_prompt, chapter_no) — fix_prompt is more actionable for LLM polish
  - reader-pull-checker output has two score fields conceptually: overall_score (existing) and score (normalized) — normalize_checker_output handles both
  - VIOLATION_FIX_TEMPLATES use {detail} placeholder for dynamic content from violation descriptions
  - config/reader-pull.yaml is the single source of truth for hook gate thresholds; avoid hardcoding
---

## 2026-04-15 - US-104
- Implemented `ink_writer/pacing/high_point_scheduler.py`: proactive cool-point recipe scheduler
- 6 cool-point types (face_slap, hidden_strength, level_up_kill, authority_challenge, villain_fail, sweet_surprise) × 3 intensity levels (minor, combo, milestone)
- Scheduling logic: base intensity from volume arc position + upgrade rules (combo/milestone overdue, climax zone, golden opening)
- Type picker: avoids overused types (repeat_limit=2 per 5-chapter window), prefers contextual types by volume position
- Gap enforcement: ≥2 consecutive no-HP chapters → force high-point next chapter
- Payoff window: minor=1ch, combo=2ch, milestone=3ch
- Wired into ink-plan SKILL.md Step 2.5 as hard constraint; chapter outline template adds `爽点配方` field
- Config: `config/high-point-scheduler.yaml` — all thresholds configurable
- 41 tests including 50-chapter simulation: density variance ≤ 0.2, no 3 consecutive without HP, type diversity ≥ 3, climax = milestone, opening = combo+
- All 1278 tests pass
- Files changed: ink_writer/pacing/__init__.py, ink_writer/pacing/high_point_scheduler.py, config/high-point-scheduler.yaml, tests/pacing/__init__.py, tests/pacing/test_high_point_scheduler.py, pytest.ini, ink-writer/skills/ink-plan/SKILL.md
- **Learnings for future iterations:**
  - Cool-point type taxonomy (6 types) is from cool-points-guide.md; aligns with high-point-checker agent detection modes
  - ink-plan chapter template is pure Markdown with `- fieldname: value` format; new fields go before/after existing ones
  - The scheduler pattern (dataclass config from YAML + pure-function scheduling + dataclass output) is reusable for future constraint engines
  - Volume position 0.85-1.0 is the climax zone; chapters 1-3 are golden opening — both get intensity boosts
  - The 50-chapter simulation test is the key acceptance test; it runs the scheduler in a feedback loop (recipe → record → next chapter)
  - config/high-point-scheduler.yaml follows same pattern as config/reader-pull.yaml
- Emotion valence/arousal model: 7 Chinese emotions → (valence, arousal) 2D mapping in EMOTION_VALENCE_AROUSAL dict
- emotion-curve-checker is conditional (not core); weight 5% in review gate; triggers ch≥5 or flat emotion history
- ink-write Step 3.7 is emotion curve gate; runs after Step 3.6 hook gate, before Step 4
- polish-agent repair priority: Step 1.5 hook → Step 1.6 emotion → Step 2 editor-wisdom
- config/emotion-curve.yaml: variance_threshold=0.15, score_threshold=60, max_retries=2
- data-agent Step B.12 extracts emotion curves to data/emotion_curves.jsonl (per-scene valence/arousal)
- Anti-detection gate: `ink_writer.anti_detection.anti_detection_gate.run_anti_detection_gate()` — sentence diversity hard gate at Step 3.8
- Zero-tolerance check runs BEFORE checker_fn; matching ZT_TIME_OPENING/ZT_MEANWHILE causes immediate block with no retry
- config/anti-detection.yaml: score_threshold=70, golden_three_threshold=80, max_retries=1, plus statistical thresholds + zero_tolerance rules
- sentence_diversity.py: pure statistical analysis (sentence CV, short/long ratio, paragraph CV, dialogue ratio, emotion punctuation density, causal density)
- polish-agent repair priority: Step 1.5 hook → Step 1.6 emotion → Step 1.7 anti-detection → Step 2 editor-wisdom
- Style RAG: `ink_writer.style_rag.StyleRAGRetriever` — semantic search over 3295 human-written scene fragments (FAISS+bge-small-zh-v1.5)
- Style RAG index files: data/style_rag/{style_rag.faiss, metadata.json, contents.json}; rebuild via `python scripts/build_style_rag.py`
- Style RAG filters: scene_type, emotion, genre, min_quality — filtered search reconstructs vectors from FAISS for subset matching
- polish-agent Step 2.8: Style RAG retrieval via `build_polish_style_pack()`; formats `【人写参考】` blocks for AI味修复 context
- RetrieverProtocol (typing.Protocol) in polish_integration.py allows test mocking without FAISS/sentence-transformers dependency
---

## 2026-04-15 - US-105
- Implemented `ink_writer/emotion/` module: config.py, emotion_detector.py, fix_prompt_builder.py, emotion_gate.py
- `emotion_detector`: scene splitting (double newline/***/--- separators, min 200 chars), keyword-based valence/arousal (7 emotions), flat segment detection, cosine similarity, curve interpolation
- `emotion_gate`: run_emotion_gate() with max-2-retry polish loop, writes `chapters/{n}/emotion_blocked.md` on failure
- `fix_prompt_builder`: 5 violation templates (EMOTION_FLAT/VARIANCE_LOW/AROUSAL_FLAT/MONOTONE/CORPUS_MISMATCH), normalize_checker_output()
- Created `emotion-curve-checker.md` agent spec: scene-level valence/arousal analysis, scoring formula, fix_prompt generation
- Updated `data-agent.md`: added Step B.12 emotion curve extraction to data/emotion_curves.jsonl
- Updated `ink-write/SKILL.md`: added Step 3.7 emotion curve gate (after Step 3.6 hook gate, before Step 4)
- Updated `step-3-review-gate.md`: added emotion-curve-checker as conditional checker (5% weight, triggers on ch≥5 or flat history)
- Updated `polish-agent.md`: added emotion_fix_prompt input + Step 1.6 emotion repair
- Config: `config/emotion-curve.yaml` — variance_threshold=0.15, score_threshold=60, max_retries=2, corpus_similarity_threshold=0.8
- 65 new tests (detector/config/gate/fix_prompt/50-chapter simulation), 1343 total pass
- Files changed: ink_writer/emotion/__init__.py, ink_writer/emotion/config.py, ink_writer/emotion/emotion_detector.py, ink_writer/emotion/emotion_gate.py, ink_writer/emotion/fix_prompt_builder.py, config/emotion-curve.yaml, ink-writer/agents/emotion-curve-checker.md, ink-writer/agents/data-agent.md, ink-writer/agents/polish-agent.md, ink-writer/skills/ink-write/SKILL.md, ink-writer/skills/ink-write/references/step-3-review-gate.md, tests/emotion/__init__.py, tests/emotion/test_emotion_engine.py, pytest.ini
- **Learnings for future iterations:**
  - Emotion valence/arousal model: 7 emotions mapped to 2D space; keyword frequency weighting is simple but effective for Chinese text
  - Scene splitting must handle very short Chinese text — min_scene_chars=200 aggressively merges; test texts need ≥200 chars per scene segment
  - The gate pattern (config dataclass + retry loop + blocked file + logger) from reader_pull is directly reusable — just change names and violation templates
  - emotion-curve-checker is a conditional checker (not core); triggers on ch≥5, flat history, or emotional arc chapters
  - Step 3.7 runs after Step 3.6 (hook gate); if hook gate already blocked, emotion gate is skipped
  - Corpus similarity uses interpolation to same-length then cosine; reference curves from first N chapters serve as comparison targets
  - polish-agent now has 3 priority repair steps: 1.5 hook → 1.6 emotion → 2 editor-wisdom
  - The fix_prompt_builder pattern (VIOLATION_FIX_TEMPLATES dict + build_fix_prompt + normalize_checker_output) is identical across gates — could be abstracted but premature for now
---

## 2026-04-15 - US-201
- Implemented Style RAG index builder + semantic retriever
- `scripts/build_style_rag.py`: reads 3295 fragments from benchmark/style_rag.db, encodes with BAAI/bge-small-zh-v1.5, builds FAISS IndexFlatIP, outputs to data/style_rag/ (faiss index + metadata.json + contents.json + build_stats.json)
- `ink_writer/style_rag/retriever.py`: StyleRAGRetriever class with full-text semantic search + filter-by-scene_type/emotion/genre/min_quality
- `ink_writer/style_rag/__init__.py`: exports StyleFragment + StyleRAGRetriever
- Reuses same model (bge-small-zh-v1.5) and FAISS pattern as editor-wisdom retriever
- Metadata separated from content: metadata.json has preview only (≤200 chars), contents.json has full text — keeps metadata load fast
- 24 tests (2 dataclass, 3 init, 7 retrieval, 7 filtered retrieval, 5 build script) + 3 live-index tests (skipped if index not built)
- All 1367 tests pass
- Files changed: scripts/build_style_rag.py, ink_writer/style_rag/__init__.py, ink_writer/style_rag/retriever.py, tests/style_rag/__init__.py, tests/style_rag/test_style_rag.py, pytest.ini, prd.json
- **Learnings for future iterations:**
  - benchmark/style_rag.db already has 3295 scene-tagged fragments from style_rag_builder.py — no need to re-slice, just build vector index on top
  - Style RAG fragments are keyed by md5(title:ch:scene_index)[:12]; same ID scheme in both SQLite and FAISS metadata
  - Full content stored separately in contents.json (not metadata.json) to keep metadata load fast; retriever joins them by id
  - StyleRAGRetriever._filtered_search reconstructs vectors from FAISS index for subset search (same pattern as editor-wisdom category filtering)
  - Module-scoped fixtures recommended for sentence-transformers tests (model load ~30s); use tmp_path_factory not tmp_path for module scope
  - The existing style_rag_builder.py classifies scenes using SCENE_TYPE_KEYWORDS and EMOTION_KEYWORDS from benchmark/config.py
  - Fragment quality_score (0-1) computed by deviation from style benchmarks (avg_sentence_length=28.6, dialogue_ratio=0.345, etc.)
---

## 2026-04-15 - US-202
- Implemented `ink_writer/style_rag/polish_integration.py`: bridge between polish-agent and Style RAG retriever
- `build_polish_style_pack()`: takes anti-detection-checker fix_priorities + chapter text, retrieves Top-K human-written snippets per fix item
- `PolishStylePack.format_full_prompt()`: formats references as `【人写参考】` blocks with stats (句长均值/对话占比/质量分)
- `_extract_paragraph_text()`: extracts query text from chapter by paragraph/line location strings
- `FIX_TYPE_TO_SCENE_HINT`: maps fix types to scene_type filters (对话同质→对话, etc.)
- Updated `polish-agent.md`: added Step 2.8 Style RAG retrieval + `style_references` input field
- Updated `ink-write/SKILL.md`: Step 4 now includes Style RAG retrieval (step 4) before AI味修复 (step 5), Anti-AI check renumbered to step 6
- Updated `ink_writer/style_rag/__init__.py`: exports PolishStylePack, StyleReference, build_polish_style_pack
- Added `tests/style_rag/test_polish_integration.py` with 27 tests (extract, StyleReference, PolishStylePack, build integration, error handling)
- All 1394 tests pass (27 new)
- Files changed: ink_writer/style_rag/polish_integration.py, ink_writer/style_rag/__init__.py, ink-writer/agents/polish-agent.md, ink-writer/skills/ink-write/SKILL.md, tests/style_rag/test_polish_integration.py, prd.json
- **Learnings for future iterations:**
  - RetrieverProtocol pattern (typing.Protocol) allows easy test mocking without depending on FAISS/sentence-transformers at test time
  - polish-agent step numbering: 1.5 hook → 1.6 emotion → 2 editor-wisdom → 2.5 review issues → 2.8 style RAG → 3 AI味修复
  - _extract_paragraph_text must handle out-of-range paragraphs gracefully (return empty → skip)
  - FakeRetriever with .calls list is the standard test pattern for retriever-dependent code
  - Style RAG integration is prompt-level (formats text for LLM context), not code-level (no direct rewrite logic)
---

## 2026-04-15 - US-203
- Implemented `ink_writer/anti_detection/` module: config.py, sentence_diversity.py, fix_prompt_builder.py, anti_detection_gate.py
- `sentence_diversity.py`: pure statistical analysis — sentence CV, mean, short/long ratio, paragraph CV, single-sentence-paragraph ratio, dialogue ratio, emotion punctuation density (!/…/？), causal word density
- `anti_detection_gate.py`: zero-tolerance pre-check + checker score gate + polish retry loop (max 1 retry)
- Zero-tolerance rules: ZT_TIME_OPENING (时间标记开头) + ZT_MEANWHILE ("与此同时"转场) — immediate block, no retry, no checker call
- Created `config/anti-detection.yaml` with all statistical thresholds documented + zero_tolerance rule definitions
- Updated `ink-write/SKILL.md`: added Step 3.8 anti-detection sentence diversity gate (after Step 3.7 emotion gate, before Step 4)
- Updated `polish-agent.md`: added `anti_detection_fix_prompt` input field + Step 1.7 anti-detection repair
- Updated `step-3-review-gate.md`: documented AI味句式多样性硬门禁 in gate rules
- 46 new tests (config 5, fix_prompt_builder 7, sentence_diversity 10, zero_tolerance 9, gate 11, integration 4), 1440 total pass
- Coverage 82.54%
- Files changed: ink_writer/anti_detection/{__init__,config,sentence_diversity,fix_prompt_builder,anti_detection_gate}.py, config/anti-detection.yaml, ink-writer/skills/ink-write/SKILL.md, ink-writer/agents/polish-agent.md, ink-writer/skills/ink-write/references/step-3-review-gate.md, tests/anti_detection/{__init__,test_anti_detection_gate}.py, pytest.ini
- **Learnings for future iterations:**
  - The gate pattern (config dataclass + zero-tolerance pre-check + retry loop + blocked file + logger) is now the third instance — could be abstracted but 3 isn't enough to justify
  - Zero-tolerance check must run BEFORE checker_fn to avoid wasting LLM calls on obviously-blocked chapters
  - ZT_TIME_OPENING regex matches against first_line only (stripped); ZT_MEANWHILE matches against full text
  - sentence_diversity.py is pure Python, no LLM dependency — can be used for fast pre-filtering before expensive checker calls
  - Chinese dialogue detection uses \u201c/\u201d (""） and \u300c/\u300d (「」) paired quotes
  - Causal density calculation: count per 200 chars (matching anti-detection-checker spec's "每200字1个" threshold)
  - polish-agent step numbering now: 1.5 hook → 1.6 emotion → 1.7 anti-detection → 2 editor-wisdom → 2.5 review issues → 2.8 style RAG → 3 AI味修复
  - max_retries=1 for anti-detection gate (vs 2 for hook/emotion) because AI-taste issues are structural and rarely fixed in 1 polish pass; if it fails, human review is more productive
---

## 2026-04-15 - US-204
- Implemented `ink_writer/cultural_lexicon/` module: config.py, loader.py, context_injection.py
- 6 genre lexicons: xianxia (310), xuanhuan (305), urban (355), scifi (310), lishi (305), youxi (305) — all ≥300 entries
- Each entry: {id, term, type, category, usage_example, context_hint}; types: idiom/slang/dialect/era_word/jargon
- `loader.py`: `load_lexicon()` + `sample_lexicon()` with chapter-seeded deterministic sampling + category diversity
- `context_injection.py`: `build_cultural_lexicon_section()` outputs Markdown "### 13. 文化语料库" section for execution pack
- `config.py`: `CulturalLexiconConfig` from `config/cultural-lexicon.yaml` — per-genre min_terms, inject_count, seed_offset
- Updated `context-agent.md`: added Step 2.6 文化语料库注入 with full execution steps
- Updated `writer-agent.md`: added 文化语料库 section for using injected lexicon during writing
- 68 new tests (7 config, 6 loader, 8 sampling, 10 injection, 7×6 live data validation), 1508 total pass
- Files changed: ink_writer/cultural_lexicon/{__init__,config,loader,context_injection}.py, config/cultural-lexicon.yaml, data/cultural_lexicon/{xianxia,xuanhuan,urban,scifi,lishi,youxi}.json, ink-writer/agents/{context-agent,writer-agent}.md, tests/cultural/{__init__,test_lexicon_injection}.py, pytest.ini
- **Learnings for future iterations:**
  - Lexicon JSON schema: {genre, version, description, total_count, entries: [{id, term, type, category, usage_example, context_hint}]}
  - `sample_lexicon()` with categories ensures at least one entry per category before random fill — good for diverse injection
  - context-agent execution pack section numbering: 13 = 文化语料库 (after existing 12 sections)
  - writer-agent references "第13板块" for cultural lexicon usage instructions
  - Live data tests are parametrized by genre (6 genres × 7 tests = 42 live tests) — validate schema, min entries, no duplicates, no empty terms, valid types, markdown output
---

## 2026-04-15 - US-301
- Implemented single-source-of-truth memory architecture (v13)
- New SQLite tables: `state_kv` (key-value singleton state), `disambiguation_log`, `review_checkpoint_entries`
- SQLStateManager: full CRUD for state_kv, disambiguation, checkpoints + `rebuild_state_dict()`/`rebuild_state_json()`
- StateManager: `_sync_state_to_kv()` syncs singleton state to SQLite after every `save_state()`
- StateManager: `_load_state()` falls back to SQLite rebuild when `state.json` is missing
- migrate.py: v8→v9 migration + `migrate_state_to_sqlite()` for SQLite data sync
- state_schema.py: StateModel default schema_version updated to 9
- init_project.py: default schema_version updated to 9
- docs/memory_architecture_v13.md: full design document
- Files changed: index_manager.py, sql_state_manager.py, state_manager.py, migrate.py, state_schema.py, init_project.py, pytest.ini, test_state_schema.py, test_init_project.py, test_hook_contract_required.py
- New files: docs/memory_architecture_v13.md, tests/memory_arch/test_memory_architecture.py
- **Learnings for future iterations:**
  - state_kv is a generic key-value store; all state.json singleton fields stored as JSON blobs under their field name as key
  - When bumping CURRENT_SCHEMA_VERSION, update ALL places that hardcode old version: state_schema.py, init_project.py, and test fixtures
  - SQLite `ORDER BY created_at DESC` with same-second timestamps is non-deterministic; use `ORDER BY id ASC` for stable insertion-order retrieval
  - The rebuild path (`rebuild_state_dict`) must produce output compatible with `_ensure_state_schema()` — include all required fields with defaults
  - `migrate_state_to_sqlite` is a separate function from `run_migrations` because it needs DataModulesConfig which requires the full project path
---

## 2026-04-15 - US-302
- Implemented `ink_writer/semantic_recall/` module: config.py, chapter_index.py, retriever.py
- ChapterVectorIndex: FAISS FlatIP index over chapter memory cards, supports incremental add + save/load
- SemanticChapterRetriever: 3-way hybrid recall (semantic Top-K ∪ entity-forced ∪ recent-N), entity overlap boosting
- Config: `config/semantic-recall.yaml` with all thresholds documented
- Build script: `scripts/build_chapter_index.py --project-root <path> [--rebuild]`
- Integration: `extract_chapter_context.py` → `_search_semantic_recall()` as priority path before legacy RAG/BM25
- context-agent.md updated with `semantic_hybrid` mode documentation
- 32 new tests (1568 total), coverage 82.53%
- Files changed: ink_writer/semantic_recall/{__init__,config,chapter_index,retriever}.py, config/semantic-recall.yaml, scripts/build_chapter_index.py, ink-writer/scripts/extract_chapter_context.py, ink-writer/agents/context-agent.md, tests/semantic_recall/{test_config,test_chapter_index,test_retriever,test_integration}.py, pytest.ini
- **Learnings for future iterations:**
  - FAISS FlatIP with normalized embeddings = cosine similarity; same pattern as style_rag
  - Reuse BAAI/bge-small-zh-v1.5 model across all vector indices (style_rag, editor-wisdom, chapter_index) for consistency
  - `_search_memory_cards_and_summaries()` uses lexical token overlap; semantic recall is strictly superior for cross-chapter continuity
  - ChapterCard.from_db_row() handles both JSON-string and native-list fields for unresolved_questions/key_facts/involved_entities
  - entity-forced recall iterates cards in reverse (newest first) for efficiency with `max_hits * 3` scan window
  - The 300-chapter simulation test validates recent-N guarantee (0 missed callbacks); entity-forced coverage is best-effort
---

## 2026-04-15 - US-303
- Implemented foreshadow lifecycle manager: `ink_writer/foreshadow/` module with config, tracker, fix_prompt_builder
- `foreshadow-tracker` agent spec: priority-aware overdue rules (P0 grace=5ch, P1 grace=10ch, P2 grace=20ch), silence detection (30ch threshold), density warnings
- `config/foreshadow-lifecycle.yaml`: tunable thresholds for overdue grace, silence, forced payoffs, injection mode
- `ink_writer/foreshadow/tracker.py`: `scan_foreshadows()` scans active foreshadows from SQLite, classifies overdue/silent, generates alerts; `build_plan_injection()` creates ink-plan payload; `build_heatmap_data()` for dashboard
- `ink_writer/foreshadow/fix_prompt_builder.py`: builds targeted fix prompts with 5 violation types (OVERDUE_CRITICAL/HIGH/MEDIUM, SILENT, DENSITY_HIGH)
- ink-plan SKILL.md: Step 2.4 foreshadow lifecycle scheduling (hard constraint), chapter outline `伏笔处置` field, forced payoff validation + hard fail
- ink-write SKILL.md: Step 0 逾期伏笔检查 upgraded from raw SQL to `foreshadow.tracker.scan_foreshadows()`
- Dashboard: `/api/plot-threads/heatmap` endpoint (bucketed by chapter range), `ForeshadowHeatmap` React component with color-coded cells (planted=blue, active=yellow, resolved=green, overdue_risk=red)
- 48 new tests (1616 total), coverage 82.32%
- 300-chapter simulation: test_300_chapter_lifecycle_zero_overdue passes (with tracker intervention, overdue=0); test_300_chapter_no_tracker_has_overdue confirms overdue accumulates without intervention
- Files changed: ink_writer/foreshadow/{__init__,config,tracker,fix_prompt_builder}.py, config/foreshadow-lifecycle.yaml, ink-writer/agents/foreshadow-tracker.md, ink-writer/dashboard/app.py, ink-writer/dashboard/frontend/src/App.jsx, ink-writer/skills/ink-plan/SKILL.md, ink-writer/skills/ink-write/SKILL.md, tests/foreshadow/{__init__,test_config,test_tracker,test_fix_prompt}.py, pytest.ini
- **Learnings for future iterations:**
  - `plot_thread_registry` already has `target_payoff_chapter` (nullable) and `priority` fields — no schema migration needed
  - `_load_active_foreshadows()` must use `sqlite3.Row` factory for dict-like access; plain fetchall returns tuples
  - `build_heatmap_data()` should still generate empty buckets when no foreshadows exist (for dashboard grid)
  - Dashboard frontend uses pure CSS for heatmap (no charting library needed); rgba opacity mapping works well for density
  - foreshadow-tracker is read-only (no DB writes); data-agent handles status updates via `plot_thread_updates`
  - Step 0 逾期伏笔检查 and canary A.6 are complementary: Step 0 is priority-aware overdue, A.6 is silence-only detection
---

## 2026-04-16 - US-304
- Implemented voice fingerprint system: `ink_writer/voice_fingerprint/` module with config, fingerprint, ooc_gate, fix_prompt_builder
- `VoiceFingerprint` dataclass: catchphrases, speech_habits, vocabulary_level, tone, dialect_markers, forbidden_expressions
- DB layer: `voice_fingerprint_json` TEXT column added to `character_evolution_ledger` via `_ensure_column`; CRUD via `save_fingerprint_to_db()` / `load_fingerprint_from_db()` / `load_all_fingerprints()`
- Auto-learn: `merge_fingerprint()` append-only merge with configurable max limits per field
- Scoring: `score_chapter_voice()` with 4 violation types: VOICE_FORBIDDEN_EXPRESSION (critical), VOICE_CATCHPHRASE_ABSENT (medium), VOICE_VOCAB_MISMATCH (medium), VOICE_INDISTINCT (medium)
- Gate: `run_voice_gate()` mirrors emotion_gate pattern — checker_fn → polish retry (max 2) → voice_blocked.md on fail
- `config/voice-fingerprint.yaml`: score_threshold=60, max_retries=2, deviation thresholds, learning config
- Migration: `migrate.py` v9→v10 adds `voice_fingerprint_config` to state.json
- Agent specs updated: ooc-checker (voice_fingerprint-based validation), polish-agent (Step 1.8 voice repair), ink-write (Step 3.9 voice gate), data-agent (voice_fingerprint persistence rules)
- 55 new tests (1671 total), coverage 82.31%
- 300-chapter simulation passes: OOC score < 5 (zero critical/high violations with proper fingerprint adherence)
- Files changed: ink_writer/voice_fingerprint/{__init__,config,fingerprint,ooc_gate,fix_prompt_builder}.py, config/voice-fingerprint.yaml, ink-writer/scripts/data_modules/index_manager.py, ink-writer/scripts/migrate.py, ink-writer/agents/{ooc-checker,data-agent,polish-agent}.md, ink-writer/skills/ink-write/SKILL.md, tests/voice_fingerprint/{__init__,test_voice_config,test_voice_fingerprint,test_voice_ooc_gate,test_voice_fix_prompt,test_voice_migration}.py, tests/memory_arch/test_memory_architecture.py, pytest.ini, prd.json
- **Learnings for future iterations:**
  - `_ensure_column` in IndexManager handles schema evolution without needing a new CREATE TABLE — use it for adding nullable columns
  - `save_character_evolution()` now accepts `voice_fingerprint` (dict) or `voice_fingerprint_json` (str) — COALESCE preserves existing fingerprint if new one is null
  - Dialogue extraction uses multiple regex patterns (「」 and "" quotes, various speech verbs like 说/道/喊/叫/笑/怒) — Chinese punctuation matters
  - `_infer_vocab_level_from_length()` is a rough heuristic; real scoring relies on the vocabulary_level field from character archive, not inference
  - The 300-chapter simulation test is lightweight (no LLM calls) — tests the scoring math, not generation quality
  - When bumping CURRENT_SCHEMA_VERSION, existing migration tests may assert old version — update them to use CURRENT_SCHEMA_VERSION constant
---

## 2026-04-16 - US-305
- Implemented plotline lifecycle tracker (明暗线追踪器)
- New module `ink_writer/plotline/` with config.py, tracker.py, fix_prompt_builder.py
- Plotlines (main/sub/dark) tracked via `plot_thread_registry` table with `thread_type='plotline'`, line_type stored in `payload_json`
- Inactivity detection: main=3章, sub=8章, dark=15章 max gap; severity: main=critical, sub=high, dark=medium
- plotline-tracker agent spec: `ink-writer/agents/plotline-tracker.md`
- ink-plan Step 2.7: plotline lifecycle scheduling (hard constraint) + `明暗线推进` chapter outline field
- ink-write Step 3.10: plotline gate after voice fingerprint gate
- Dashboard: `/api/plotlines` (list) + `/api/plotlines/heatmap` (heatmap by chapter bucket)
- migrate.py v10→v11: adds `plotline_lifecycle_config` to state.json
- state_schema.py: added `plotline_registry` field to PlotThreads model
- config/plotline-lifecycle.yaml: all thresholds documented
- 56 new tests (1727 total), 300-chapter simulation passes, coverage 82.24%
- Files changed: ink_writer/plotline/{__init__,config,tracker,fix_prompt_builder}.py, config/plotline-lifecycle.yaml, ink-writer/agents/plotline-tracker.md, ink-writer/skills/ink-plan/SKILL.md, ink-writer/skills/ink-write/SKILL.md, ink-writer/scripts/{state_schema,migrate}.py, ink-writer/dashboard/app.py, tests/plotline/{__init__,test_plotline_config,test_plotline_tracker,test_plotline_fix_prompt,test_plotline_migration}.py, tests/voice_fingerprint/test_voice_migration.py, pytest.ini
- **Learnings for future iterations:**
  - Plotlines reuse `plot_thread_registry` table via `thread_type='plotline'` — no new table needed, just filter on thread_type
  - `payload_json` is the extension point for type-specific data (line_type for plotlines, other fields for foreshadowing)
  - When adding new outline fields, update 3 places in ink-plan: chapter format template, final check list, and hard fail conditions
  - Dashboard heatmap reuses the tracker module's `build_plotline_heatmap()` — pass db_path not conn to avoid lifecycle issues
  - 300-chapter simulation tests are fast (~0.1s) because they only test scan logic, not LLM generation
---

## 2026-04-16 - US-401
- Implemented Agent 职责重映射 based on US-003 audit findings
- Merged `foreshadow-tracker` + `plotline-tracker` → `thread-lifecycle-tracker` (identical state machine, scoring model, ink-plan interaction)
- Created `ink-writer/references/shared-checker-preamble.md` extracting common checker input/output/scoring rules
- Added 6 missing metrics schemas to `checker-output-schema.md` (emotion-curve, anti-detection, proofreading, golden-three, thread-lifecycle, editor-wisdom)
- Updated `ink-plan/SKILL.md` and `ink-write/SKILL.md` references from old trackers to unified tracker
- Created `docs/agent_topology_v13.md` with before/after diagrams, pipeline architecture, responsibility matrix
- Documented 7 overlap pairs as false positives (shared boilerplate, not functional duplication)
- Regenerated `reports/architecture_audit.md` (now 19 agents, 9 overlap pairs, 50 prompt fragments)
- Files changed: docs/agent_topology_v13.md, ink-writer/agents/thread-lifecycle-tracker.md, ink-writer/references/shared-checker-preamble.md, ink-writer/references/checker-output-schema.md, ink-writer/skills/ink-plan/SKILL.md, ink-writer/skills/ink-write/SKILL.md, reports/architecture_audit.md, tests/audit/test_agent_topology.py
- **Learnings for future iterations:**
  - Most "overlap pairs" in the audit are false positives from shared boilerplate text, not functional duplication — only merge when the state machine, scoring model, and external interaction patterns are truly identical
  - The review gate (step-3-review-gate.md) only governs Step 3 checkers; trackers (foreshadow/plotline) run at different pipeline stages (Step 0 and Step 3.10 in ink-write, Steps 2.4/2.7 in ink-plan)
  - Agent specs are markdown instructions for Claude subagents; "merging" means creating a unified spec with a type parameter, while Python modules can remain separate
  - checker-output-schema.md summary format should list ALL active checkers (was missing 6); keep this in sync when adding new agents
  - Old agent files retained for backward compatibility — agent name aliasing ensures no breakage
---

## 2026-04-16 - US-402
- Eliminated dual agent directory: moved editor-wisdom-checker.md from agents/ink-writer/ to ink-writer/agents/
- Removed empty agents/ directory
- Files changed: CLAUDE.md, docs/agent_topology_v13.md, scripts/audit_architecture.py, tests/audit/test_agent_topology.py, ink-writer/agents/editor-wisdom-checker.md
- **Learnings for future iterations:**
  - Only 1 agent (editor-wisdom-checker) was in the secondary directory; all others were already in ink-writer/agents/
  - audit_architecture.py has AGENT_DIRS in two places (module-level constant + function default) — update both
  - Test file test_agent_topology.py explicitly asserted the secondary directory existed — tests that assert directory structure need updating when directories are reorganized
---

## 2026-04-16 - US-403
- What was implemented: Decision document for ink-writer vs webnovel-writer skill system merge
- Decision: Merge — ink-writer is the sole system, webnovel-writer deprecated
- Files changed:
  - docs/skill_systems_decision.md (new): full comparison analysis + 4-phase migration timeline
  - tests/skill_systems/test_skill_systems_decision.py (new): 31 tests validating decision doc completeness + ink-writer superset coverage
  - tests/skill_systems/__init__.py (new)
  - pytest.ini: added tests/skill_systems to testpaths
- **Learnings for future iterations:**
  - ink-writer skills are at `ink-writer/skills/`, webnovel-writer at `~/.claude/plugins/marketplaces/webnovel-writer-marketplace/webnovel-writer/skills/`
  - migrate_webnovel_to_ink.sh already exists and handles .webnovel→.ink + volume flattening
  - webnovel-writer's --fast/--minimal modes are not yet in ink-write — noted as Phase B backport item
  - Shared data_modules are byte-identical between both systems; ink-writer adds 6 extra modules
  - Core references (core-constraints.md, strand-weave-pattern.md, cool-points-guide.md) are byte-identical
---

## 2026-04-16 - US-404
- Implemented: Prompt template centralization with version tracking and A/B test capability
- Files created:
  - `ink-writer/templates/prompts/` — 10 versioned shared prompt templates + `_manifest.json`
  - `scripts/ab_prompts.py` — A/B test harness with list/diff-all/show/compare commands
  - `tests/prompts/test_prompt_templates.py` — 24 tests for template system
- Files modified:
  - 15 agent specs updated with `{{PROMPT_TEMPLATE:...}}` references (31 total refs)
  - `scripts/audit_architecture.py` — template-aware n-gram dedup
  - `ink-writer/references/shared-checker-preamble.md` — marked as migrated
  - `tests/audit/test_agent_topology.py` — updated for template refs
  - `pytest.ini` — added tests/prompts testpath
- Templates extracted:
  - checker-input-rules.md (13 consumers)
  - checker-output-reference.md (10 consumers)
  - checker-range-input.md (5 consumers)
  - checker-load-context.md (3 consumers)
  - checker-report-template.md (5 consumers)
  - responsibility-boundary.md (2 consumers)
  - iron-laws.md (2 consumers)
  - knowledge-boundary-rules.md (2 consumers)
  - tracker-shared-logic.md (3 consumers)
  - polish-priority-rules.md (1 consumer)
- **Learnings for future iterations:**
  - Agent specs are Claude Code subagent markdown — "prompt extraction" means replacing inline duplicate blocks with `{{PROMPT_TEMPLATE:...}}` references, not Python code changes
  - The `{{SHARED_CHECKER_PREAMBLE}}` pattern in thread-lifecycle-tracker was the precedent for template references
  - The n-gram dedup in audit_architecture.py counts template reference markers as duplicates — must strip them before analysis
  - 4 agents (anti-detection-checker, proofreading-checker, context-agent, data-agent) have unique enough content that no template extraction is needed
  - agent-specific "默认数据源" lines must remain inline since each agent uses different data
---

## 2026-04-16 - US-501
- What was implemented: 章节级并发管线 — ink-auto --parallel N 支持
- Files changed:
  - ink_writer/parallel/__init__.py (new)
  - ink_writer/parallel/chapter_lock.py (new) — SQLite-based cross-process chapter locks with TTL
  - ink_writer/parallel/pipeline_manager.py (new) — asyncio PipelineManager orchestrator
  - config/parallel-pipeline.yaml (new) — parallel settings
  - ink-writer/scripts/ink-auto.sh — argument parsing for --parallel, parallel dispatch block
  - ink-writer/skills/ink-auto/SKILL.md — documented --parallel usage
  - pytest.ini — added tests/parallel, asyncio_mode=auto
  - tests/parallel/test_chapter_lock.py (new) — 12 lock tests
  - tests/parallel/test_pipeline_manager.py (new) — 22 pipeline tests
- **Learnings for future iterations:**
  - asyncio_mode=auto in pytest.ini is needed for @pytest.mark.asyncio tests to work without per-test markers
  - ChapterResult.elapsed must handle start_time=0 edge case (test with time=0 fixtures)
  - SQLite WAL + busy_timeout already existed in IndexManager; parallel_locks.db is a separate coordination DB
  - The batch-parallel model (write N → checkpoint → next batch) is simpler than true pipeline parallelism and avoids complex inter-stage dependencies
---

## 2026-04-16 - US-502
- What was implemented: Anthropic prompt cache 优化 — cache_control + 指标追踪
- Files changed:
  - ink_writer/prompt_cache/__init__.py (new)
  - ink_writer/prompt_cache/config.py (new) — PromptCacheConfig with stable/volatile segment lists
  - ink_writer/prompt_cache/segmenter.py (new) — CacheableMessage, segment_system_prompt, build_cached_system_param
  - ink_writer/prompt_cache/metrics.py (new) — CacheMetricsTracker SQLite-backed
  - ink_writer/editor_wisdom/llm_backend.py — cache_control on system param + _record_cache_metrics
  - ink_writer/editor_wisdom/checker.py — cached_system block for direct anthropic_client calls
  - config/prompt-cache.yaml (new) — 7 stable + 5 volatile segments
  - tests/prompt_cache/test_prompt_cache.py (new) — 30 tests
  - pytest.ini — added tests/prompt_cache
- **Learnings for future iterations:**
  - Main writing pipeline uses Claude Code Task tool, not direct API calls — cache_control applies only to editor-wisdom direct SDK calls
  - PromptCacheConfig uses dataclass field(default_factory=...) — can't reference class attributes in load_config, need defaults instance
  - CacheMetricsTracker stores per-call metrics so cache_hit_rate can be computed over any window
  - Anthropic cache_control format: [{"type": "text", "text": ..., "cache_control": {"type": "ephemeral"}}]
---

## 2026-04-16 - US-503
- What was implemented: 检查器并行化 + 早失败终止
- Files changed:
  - ink_writer/checker_pipeline/__init__.py (new)
  - ink_writer/checker_pipeline/runner.py (new) — CheckerRunner asyncio executor with GateSpec
  - ink-writer/skills/ink-write/references/step-3-review-gate.md — full parallelism dispatch
  - tests/checker_pipeline/test_checker_runner.py (new) — 25 tests
  - pytest.ini — added tests/checker_pipeline
- **Learnings for future iterations:**
  - Sync checker functions with time.sleep() block the event loop — must use asyncio.to_thread() for true parallelism
  - asyncio.iscoroutinefunction deprecated in 3.16; use inspect.iscoroutinefunction() instead
  - cancel_event pattern: set event on hard-gate fail, check at semaphore acquire — doesn't cancel in-progress to_thread tasks but prevents new ones from starting
  - Thread-based tasks (to_thread) can't be truly cancelled mid-sleep; timing tests must be tolerant
---
