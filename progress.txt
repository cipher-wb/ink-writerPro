# Ralph Progress — token-optimization

## Codebase Patterns
- Agent spec files: ink-writer/agents/*.md (21 files, ~265KB total)
- Main workflow: ink-writer/skills/ink-write/SKILL.md (~2000 lines)
- Review gate: ink-writer/skills/ink-write/references/step-3-review-gate.md
- Style adapter: ink-writer/skills/ink-write/references/style-adapter.md
- Shared refs: ink-writer/references/shared/ (11 files, ~58KB)
- Prompt templates: ink-writer/templates/prompts/ (11 files, ~10KB)
- Scripts: ink-writer/scripts/ (workflow_manager.py, ink.py, etc.) + scripts/ (verify_optimization_quality.py etc.)
- Tests: tests/ directory, pytest with pythonpath=. ink-writer/scripts ink-writer/dashboard scripts
- Review report JSON structure: overall_score (float), checker_results (dict→{overall_score, issues}), review_payload_json.entity_count, severity_counts
- Data-agent output has entities_appeared + entities_new for entity extraction counts
- Step 3 has 7 core checkers (always-on) + 6 conditional checkers
- Review bundle generated by: ink.py extract-context --format review-pack-json
- Slim bundle generation: slim_review_bundle.py --bundle X --checkers Y --outdir Z (per-checker field subsetting with fallback)
- Logic precheck: ink-writer/scripts/logic_precheck.py provides precheck_arithmetic() and precheck_attributes()
- Slim bundle precheck injection: slim_review_bundle.py --precheck flag runs logic_precheck and injects into logic-checker bundle
- Step 2B uses style-adapter.md, has dual-mode (full/targeted) based on step2b_metrics.py
- step2b_metrics.py: evaluate() returns {avg_sentence_length, dialogue_ratio, targeted_mode, long_sentences, summary_phrases}
- context-agent token budget: 8000(ch1-3) / 7000(ch4-30) / 9000(ch31-100) / 13000(ch101+), cap 15000
- data-agent.md is 792 lines, outputs entities/relations/summaries
- ai-word-blacklist.md at ink-writer/references/shared/ai-word-blacklist.md (49 lines)

---

## 2026-04-16 - US-001
- Implemented `scripts/verify_optimization_quality.py` — compares review reports before/after optimization
- Compares 4 dimensions: overall_score (≤2), per-checker scores (≤3), issues count (≤+1), entity extraction (≤5%)
- Outputs markdown PASS/FAIL report with detailed comparison table
- Also loads data-agent payload files for entity extraction counts
- Files changed: scripts/verify_optimization_quality.py, tests/baseline/test_verify_optimization_quality.py
- **Learnings for future iterations:**
  - Review report JSON: `overall_score` at top level, `checker_results` maps checker name → `{overall_score, issues: [...]}`, entity counts in `review_payload_json.entity_count`
  - Data-agent outputs `entities_appeared` + `entities_new` in its payload JSON
  - pytest.ini `pythonpath` includes both `scripts` and `ink-writer/scripts` — imports from either location work
  - ruff auto-fix (`--fix`) handles import sorting and unused import cleanup
---

## 2026-04-16 - US-002
- Implemented per-checker slim review bundles to reduce redundant data transfer in Step 3
- Added checker input profile mapping table to `step-3-review-gate.md` defining 7 core + 6 conditional checker field needs
- Created `ink-writer/scripts/slim_review_bundle.py` — takes full bundle, extracts field subsets per checker profile
- Updated `SKILL.md` Step 3 workflow to generate slim bundles after full bundle, with fallback to full bundle on error
- Task parameter passing updated: `review_bundle_file` now points to per-checker slim bundle
- Files changed: ink-writer/scripts/slim_review_bundle.py, ink-writer/skills/ink-write/references/step-3-review-gate.md, ink-writer/skills/ink-write/SKILL.md, tests/baseline/test_slim_review_bundle.py
- **Learnings for future iterations:**
  - Review bundle has 20+ top-level fields; anti-detection-checker only needs chapter_text + meta (~5 fields vs 20+)
  - consistency-checker is the heaviest profile (7 content fields); anti-detection-checker is lightest (1 content field)
  - Conditional checkers don't have their own profiles — they map to the nearest core checker's profile
  - META_FIELDS (chapter, project_root, chapter_file, etc.) must always be preserved for all checkers
  - Fallback design: per-checker failure returns original bundle path, not an error — workflow continues unblocked
---

## 2026-04-16 - US-003
- Implemented `ink-writer/scripts/logic_precheck.py` — L1 (arithmetic) and L3 (attribute consistency) pre-checks
- `precheck_arithmetic()`: extracts number sequences (countdown, money, proximity), validates arithmetic consistency
- `precheck_attributes()`: extracts character occupations and gender pronouns, cross-validates within chapter
- Updated `slim_review_bundle.py` to accept `--precheck` flag and `per_checker_extras` parameter for injecting precheck results into logic-checker's slim bundle
- Updated `logic-checker.md` with Step 1.5 to consume `precheck_results` field (reference only, LLM still runs full 8-layer check)
- Updated `SKILL.md` Step 3 slim bundle generation to include `--precheck` flag
- Files changed: ink-writer/scripts/logic_precheck.py (new), ink-writer/scripts/slim_review_bundle.py, ink-writer/agents/logic-checker.md, ink-writer/skills/ink-write/SKILL.md, tests/baseline/test_logic_precheck.py (new)
- **Learnings for future iterations:**
  - Countdown deviation must only flag when narrative < gap (not when narrative > gap — that means content is sufficient)
  - Character attribute extraction needs sentence-level context (not just post-name window) to catch "张伟是个程序员" patterns
  - `slim_bundle()` `extra_fields` parameter cleanly injects precheck without changing per-checker profile definitions
  - ruff SIM110 requires replacing for-loop-with-return-True patterns with `any()` expressions
---

## 2026-04-16 - US-004
- Implemented Step 2B conditional downgrade to targeted check mode
- Created `ink-writer/scripts/step2b_metrics.py` — calculates sentence length avg and dialogue ratio
  - `evaluate()`: returns metrics + mode decision (targeted/full) + pre-detected issues
  - `find_long_sentences()`: detects >55 char non-dialogue sentences
  - `find_summary_phrases()`: detects AI trace phrases (由此可见, 换句话说, etc.)
- Updated `SKILL.md` Step 2B: added pre-computation step and dual-mode branch (Mode A: full, Mode B: targeted)
- Updated `style-adapter.md`: replaced "必要性评估" with structured dual-mode spec (trigger conditions, responsibilities)
- Files changed: ink-writer/scripts/step2b_metrics.py (new), ink-writer/skills/ink-write/SKILL.md, ink-writer/skills/ink-write/references/style-adapter.md, tests/baseline/test_step2b_metrics.py (new)
- **Learnings for future iterations:**
  - style-adapter.md already had informal skip conditions (v10.6); formalizing into code + spec makes it deterministic
  - Chinese sentence splitting by 。？！ is sufficient; English periods within Chinese text are rare in this codebase
  - Dialogue uses 「」 (not "" or '') in this project — regex must match these specific brackets
  - Summary phrases list should match ai-word-blacklist.md patterns but focus on meta-narrative markers specifically
---

## 2026-04-16 - US-005
- Added Step 5.5 "输出后处理 — 空值字段自适应裁剪" to `ink-writer/agents/context-agent.md`
- Trim rules: null, "", [], {}, "not_specified" fields auto-removed from execution pack output
- Required-keep list: chapter_num, chapter_goal, required_entities, time_budget (always output even if empty)
- Nested trimming: recursive removal of empty fields within objects; empty parent objects removed if not in keep list
- Trim logging: `<!-- context-pack-trim: removed {N} empty fields, kept {M} required fields -->` appended to pack
- Degradation fallback: if Step 6 red-line fails after trim, revert to untrimmed version
- Added 2 new success criteria (#23, #24) to validate trim execution
- Files changed: ink-writer/agents/context-agent.md
- **Learnings for future iterations:**
  - context-agent.md is pure agent spec (no Python code) — changes are documentation-only, no new tests needed
  - Step 5.5 sits between Step 5 (assembly) and Step 6 (red-line validation) — natural checkpoint for output optimization
  - MCC "not_specified" fields are safe to trim because writer-agent's self-check already handles missing MCC fields
  - The 4 required-keep fields were chosen because writer-agent uses them as structural anchors even when empty
---

## 2026-04-16 - US-006
- Added pure JSON output hard constraints to `ink-writer/agents/data-agent.md`
- New "输出格式硬约束" section with 5 rules: pure JSON only, no markdown fences, no analysis text, no redundant notes in values, strict schema compliance
- Added correct vs incorrect output examples to make the constraint unambiguous
- Updated Step J reporting: slow-run explanations (>30s) now go in `warnings` array instead of free text
- Updated success criteria: #9 strengthened to "纯 JSON（无 markdown 围栏、无解释文字）", added #10 for `json.loads()` parseability
- Files changed: ink-writer/agents/data-agent.md
- **Learnings for future iterations:**
  - data-agent.md is pure agent spec (792→821 lines), no Python code — changes are documentation-only
  - Step J "报告文字部分" was ambiguous — now clarified to use `warnings` array within JSON
  - The output section sits between "输入" and "StateChange 标准化枚举" sections (lines 41-95)
---

## 2026-04-16 - US-007
- Reorganized SKILL.md References section into 3 cache-tiered batches: 静态(跨章不变) → 半静态(跨卷不变) → 动态(每章变化)
- Added cache optimization rationale comment at top of References section
- Updated Step 2A and Step 4 loading blocks in SKILL.md with batch comments
- Updated writer-agent.md loading order with cache tier comments (core-constraints=static, anti-detection=semi-static)
- Updated polish-agent.md loading order with cache tier comments (polish-guide+typesetting=static)
- Zero content changes — only reordered declarations and added explanatory comments
- Files changed: ink-writer/skills/ink-write/SKILL.md, ink-writer/agents/writer-agent.md, ink-writer/agents/polish-agent.md
- **Learnings for future iterations:**
  - SKILL.md References section was previously unordered; now grouped by cache tier for prompt cache optimization
  - writer-agent and polish-agent already had good static-first order; main value was adding explicit comments and SKILL.md restructuring
  - scene-craft-index.md is loaded on-demand within writer-agent (not at agent init), so its position doesn't affect cache
  - PROMPT_TEMPLATE references (iron-laws, responsibility-boundary, knowledge-boundary-rules) are injected at parse time, not runtime — their order in the .md file matters for readability but less for cache
---
