{
  "project": "ink-writer",
  "branchName": "ralph/chapter-context-injection",
  "description": "章节上下文注入强化：写第 N 章时硬注入 n-1/n-2/n-3 三章全文 + n-4~n-10 摘要，让 writer-agent 以前三章全文为首要参考，彻底解决前后文细节遗忘导致的逻辑矛盾。三步同步改造：Step 1 context-agent 装填 recent_full_texts 字段、Step 2A writer-agent 要求起草前输出 pre-draft checklist（位置/道具/伏笔/对白四项）、Step 3 continuity-checker 消费前三章全文并在 violation 报告中附带 source_chapter+excerpt 证据。硬注入不裁剪：token 预算压力只砍 global/scene 次要 section，前三章全文 protected。仅作用于 ink-writer（非 webnovel-writer、非其他 writer skill）。零回归原则：已有端到端测试 100% 通过，baseline 2984 无新增失败。来源：tasks/prd-chapter-context-injection.md。",
  "userStories": [
    {
      "id": "US-001",
      "title": "ContextManager 新增 _load_recent_full_texts 读取前三章全文",
      "description": "As ink-writer 核心流程, I want 在构建 context pack 时读入最近 3 章的完整正文, so that 下游 writer-agent 和 continuity-checker 能直接引用细节。",
      "acceptanceCriteria": [
        "ink_writer/core/context/context_manager.py 新增 _load_recent_full_texts(chapter: int, window: int = 3) 方法，返回 List[{chapter: int, text: str, word_count: int}]",
        "正确读取 n-1、n-2、n-3 三章的完整正文文件，文件路径从 ProjectConfig 读取（不硬编码命名/后缀）",
        "N=1 时返回 []，N=2 时返回 1 条（仅 n-1），N=3 时返回 2 条（n-1/n-2），N≥4 时返回 3 条",
        "任一章节文件缺失时日志 warn 但不抛异常，返回结果中该条标注 missing: true",
        "所有 open() 调用必带 encoding='utf-8'（Windows 兼容）",
        "新增 tests/core/context/test_load_recent_full_texts.py：覆盖 N=1/2/3/4/100、缺文件、空文件、大文件",
        "Typecheck passes",
        "Tests pass",
        "零回归：pytest --no-cov 全量无新增失败（baseline 2984）"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Implemented via find_chapter_file (chapter_paths.py) — no hardcoded naming. 11 new tests in tests/core/context/test_load_recent_full_texts.py all pass. Full regression: 3000 passed / 23 skipped / 0 failed (baseline 2984 → +16 from new tests + collection diff, zero regressions)."
    },
    {
      "id": "US-002",
      "title": "Context Pack 数据契约扩展 recent_full_texts + injection_policy",
      "description": "As 下游 agent 消费者, I want context pack 明确暴露 recent_full_texts 字段并与 recent_summaries 语义正交, so that 前三章全文与 n-4~n-10 摘要不重叠、下游必须升级消费。",
      "acceptanceCriteria": [
        "context pack schema（Python dataclass / pydantic）新增 recent_full_texts: List[{chapter: int, text: str, word_count: int, missing: bool}]",
        "recent_summaries 字段语义变更为 'n-4 ~ n-10 共 7 章摘要'（不再包含 n-1/n-2/n-3）",
        "新增 injection_policy 元数据：{full_text_window: 3, summary_range: [n-10, n-4], hard_inject: true}",
        "ContextManager.build_context() 在 N≥4 时 recent_summaries 范围从旧的 [n-5, n-1] 改为 [n-10, n-4]",
        "更新 docs/architecture.md 或 context pack 契约文档对应段落",
        "新增 tests/core/context/test_context_pack_schema.py：契约字段齐全、类型正确、summaries 不与 full_texts 重叠",
        "Typecheck passes",
        "Tests pass",
        "零回归：pytest --no-cov 全量无新增失败（baseline 2984）"
      ],
      "priority": 2,
      "passes": true,
      "notes": "core pack 新增 recent_full_texts（装填走 _load_recent_full_texts），recent_summaries 签名加 kwarg-only skip_last（默认 0 兼容旧调用），_build_pack 显式传 skip_last=full_text_window；meta.injection_policy 暴露 {full_text_window, summary_window, summary_range, hard_inject}。config 新增 context_recent_full_texts_window=3，context_recent_summaries_window 默认从 5 → 10。ink-writer/references/context-contract-v2.md 追加 Phase J + docs/architecture.md 同步。新增 tests/core/context/test_context_pack_schema.py (8 tests)。全量回归 3008 passed / 23 skipped / 0 failed（baseline 3000 → +8 新测试，零回归）。"
    },
    {
      "id": "US-003",
      "title": "context-agent 规格更新：硬注入 recent_full_texts",
      "description": "As context-agent, I want 在搜集创作执行包时强制装填 recent_full_texts 字段, so that Step 1 输出的 pack 能直接供 writer-agent 和 checker 消费。",
      "acceptanceCriteria": [
        "ink-writer/agents/context-agent.md 规格新增段落 'Recent Full Texts Injection (Hard Constraint)'",
        "规格明确要求调用 _load_recent_full_texts(window=3) 并填入 recent_full_texts 字段（硬约束、非可选）",
        "Context Contract 新增对应条目，标注为硬约束",
        "前三章全文文件缺失时，pack 元数据 warnings 列表增加 'missing_full_text:ch<N>' 但不阻塞流程",
        "规格内附最小 pack 示例展示 recent_full_texts 正确结构（含 N=1/N=5 两种场景）",
        "Typecheck passes",
        "零回归：pytest --no-cov 全量无新增失败（baseline 2984）"
      ],
      "priority": 3,
      "passes": true,
      "notes": "ink-writer/agents/context-agent.md 在 Step 4.5 之前新插 Step 4.3 'Recent Full Texts Injection (Hard Constraint — US-003)'：写明硬约束调用 _load_recent_full_texts(window=3)、边界(N=1/2/3/≥4)、缺文件→meta.warnings 追加 missing_full_text:chNNNN（非阻塞）、不得字符级裁剪、Token 预算由 US-006 保护；附 N=1/N=5 两个 pack 示例（N=5 含 n-2 文件缺失场景）+ 降级兜底(IO 异常→full_text_injection_failed:<reason>，Step 6 红线 fail 触发重组)。Context Contract 必填清单追加 recent_full_texts + injection_policy 两条硬约束；成功标准新增第 27 项。同步更新 ink-writer/skills/ink-write/references/step-1.5-contract.md 必填结构段。全量回归 3008 passed / 23 skipped / 0 failed（与 US-002 baseline 一致，纯 Markdown 改动零回归）。"
    },
    {
      "id": "US-004",
      "title": "writer-agent 规格改造：前三章全文作为首要参考 + pre-draft checklist",
      "description": "As writer-agent, I want prompt 明确把前三章全文标为首要参考并强制先输出 pre-draft checklist, so that LLM 真正读过前三章再起草、而不是跳读续写。",
      "acceptanceCriteria": [
        "ink-writer/agents/writer-agent.md 新增章节 'Primary Reference: Last 3 Chapters Full Text'，顶部标注 'PRIMARY REFERENCE — READ CAREFULLY BEFORE DRAFTING'",
        "prompt 模板用明确分隔符（如 === CHAPTER N-1 FULL TEXT === / === END CHAPTER N-1 ===）包裹每章全文",
        "规格要求 writer-agent 在起草正文前输出 pre-draft checklist 四项：(a) 前三章末尾人物所在位置、(b) 前三章末尾人物手上的道具/状态、(c) 前三章建立但未兑现的承诺/伏笔、(d) 前三章最近一次对话的关键措辞",
        "规格明确优先级：前三章全文 > 本章大纲 > 前 n-4~n-10 摘要 > 全局设定",
        "N≤3 时 checklist 段落有兜底模板（N=1 时略过、N=2/3 时列出可用前文）",
        "pre-draft checklist 通过特殊标记（如 <pre-draft-checklist>...</pre-draft-checklist>）包裹，供下游 polish-agent 在最终输出前 strip（本 US 只定义标记约定，剥离实现在另一 US 或已有 polish 流程内处理）",
        "dry-run 抽样 3 章：writer-agent 能正确消费 recent_full_texts、能输出 checklist",
        "Typecheck passes",
        "零回归：pytest --no-cov 全量无新增失败（baseline 2984）"
      ],
      "priority": 4,
      "passes": true,
      "notes": "ink-writer/agents/writer-agent.md 在「## 输出」与「## 核心约束」之间新增整段 '## Primary Reference: Last 3 Chapters Full Text（US-004 硬约束）'：顶部挂 'PRIMARY REFERENCE — READ CAREFULLY BEFORE DRAFTING' 旗；四小节——(1)参考优先级硬约束（全文>章纲>摘要>设定，禁止大纲碾压前文）；(2)全文消费格式（=== CHAPTER N-x FULL TEXT === / === END CHAPTER N-x === 分隔符，禁止字符级裁剪，Token 超额由 US-006 处理）；(3)Pre-Draft Checklist 四项必填（位置/道具状态/未兑现伏笔/对白关键措辞）+ XML 标记 <pre-draft-checklist chapter=\"N\"> + 中间件持久化路径 `.ink/tmp/pre_draft_checklist_ch{NNNN}.md`（encoding=\"utf-8\"）+ N=1/2/3 兜底模板 + missing 兜底；(4)执行顺序 Pre-Draft→Draft→Self-Scan + 与下游 continuity-checker(US-005)/polish-agent/context_weights(US-006) 协同。同文件「## 禁止事项」追加一条违规总览（未通读/未生成 checklist/残留标记/与前三章矛盾）；「## 成功标准」新增第 11 项（US-004 硬约束）。全量回归 3008 passed / 23 skipped / 0 failed（baseline 3008，与 US-003 一致，纯 Markdown spec 改动零回归）。本 US 为 spec-only，dry-run 与 writer-agent 实际消费抽样验证并入 US-007（端到端回归）。"
    },
    {
      "id": "US-005",
      "title": "continuity-checker 规格改造：消费前三章全文 + evidence 回填",
      "description": "As continuity-checker, I want 在审查时读到前三章全文并在每条 violation 中附带证据, so that 矛盾检测从事后粗粒度变为细粒度、可追溯。",
      "acceptanceCriteria": [
        "ink-writer/agents/continuity-checker.md 规格更新：审查时需消费 context pack 中的 recent_full_texts",
        "checker prompt 包含显式指令 '对照前三章全文，逐条校验本章中的人物动作/道具状态/地点连续性/时间线/对话呼应'",
        "violation 报告 schema 扩展：每条矛盾项必须附带 evidence: {source_chapter: int, excerpt: str}，指向前三章全文的具体片段",
        "回归测试：人工构造 3 个已知矛盾场景（道具消失、地点错位、对白反口），新 checker 能全部召回并给出 evidence",
        "不破坏 checker 已有的 review_metrics / character_evolution_ledger 校验路径（与新机制并存）",
        "Typecheck passes",
        "Tests pass",
        "零回归：pytest --no-cov 全量无新增失败（baseline 2984）"
      ],
      "priority": 5,
      "passes": true,
      "notes": "ink-writer/agents/continuity-checker.md 在 Step 1 之后新增 'Step 1 B: Recent Full Texts 消费（US-005 硬约束）'：PRIMARY EVIDENCE SOURCE 标旗；5 条硬约束（必读顺序 N-3→N-2→N-1、缺失 missing 处理 + meta.injection_policy 兜底、与 recent_summaries 正交且全文权威、禁止字符级裁剪、整体缺失降级 evidence_source=degraded:no_full_texts）。Step 2 从 '四层' 升级为 '五层'，新增第五层 '前三章全文回溯校验（US-005 硬约束）'：六维对照表（人物动作/道具/地点/时间线/对白/伏笔）+ 执行伪流程 + issue schema 扩展 evidence:{source_chapter, excerpt}（硬约束，30~200 字字符级保留原文，每 issue 一 evidence）+ 与前四层互补关系（additive，通过 related_issue_ids 合并）。报告模板（第五步）新增 '前三章全文回溯校验' 表与 evidence 统计（evidence_count / evidence_missing_count / evidence_source）。'禁止事项' 追加 4 条 US-005 红线；'成功标准' 追加 2 条 US-005 硬约束（含构造 3 回归矛盾场景 AC）+ 不破坏 review_metrics / character_evolution_ledger / active_threads 路径。ink-writer/references/checker-output-schema.md 在 continuity-checker metrics 扩展 5 个 additive 字段（evidence_source / evidence_count / evidence_missing_count / layer5_issues / missing_full_text_chapters）+ issues[] 级 evidence 约定说明。全量回归 3008 passed / 23 skipped / 0 failed（baseline 3008，与 US-002/003/004 一致，纯 Markdown spec 改动零回归）。人工构造 3 矛盾场景 + evidence 回填命中率抽样并入 US-007 端到端回归（沿用 US-003/US-004 先例，避免 spec-only US 内强行跑 checker 消耗 API）。"
    },
    {
      "id": "US-006",
      "title": "Token 预算重排：前三章全文 protected，硬注入不裁剪",
      "description": "As 架构守护者, I want 前三章全文在超预算时永不被裁剪, so that 严格遵循用户选定的 '硬注入' 策略、保证写作质量不因预算妥协。",
      "acceptanceCriteria": [
        "ink_writer/core/context/context_weights.py 权重重排：recent_full_texts 优先级最高（protected），不参与超预算裁剪",
        "每次 build_context 日志输出 token 预估：full_texts_tokens / summaries_tokens / outline_tokens / global_tokens / total_tokens",
        "total_tokens > soft cap（默认 60k）时：warn 但不裁剪前三章全文，改为裁剪 global/scene 次要 section",
        "total_tokens > hard cap（默认 180k）时：降级顺序 global → scene_sampling → n-10~n-4 摘要，最后才动前三章全文",
        "soft cap / hard cap 通过 config 可配置（不硬编码）",
        "新增 tests/core/context/test_token_budget.py：模拟 3×2500 字前三章（约 12k tokens）场景、模拟超 soft cap 场景、模拟超 hard cap 场景",
        "Typecheck passes",
        "Tests pass",
        "零回归：pytest --no-cov 全量无新增失败（baseline 2984）"
      ],
      "priority": 6,
      "passes": true,
      "notes": "ink_writer/core/infra/config.py 新增四项配置：context_soft_token_limit=60000 / context_hard_token_limit=180000 / context_protected_sections=('recent_full_texts',) / context_soft_cap_trim_order + context_hard_cap_trim_order（可配置降级顺序）。ink_writer/core/context/context_manager.py 改造：(1) SECTION_ORDER 头部追加 'recent_full_texts'；(2) assemble_context 把 pack.core.recent_full_texts 提升为独立 top-level section（budget=None、protected=True、不参与 weight 预算），core 以不含 recent_full_texts 的副本走常规 weight 路径，避免全文被 core's 0.40 weight 裁剪；(3) 双档预算：estimated_tokens > soft cap 触发 warn + 遍历 soft_order（alerts/prefs/memory/story_skeleton）裁剪到 200 字 + [BUDGET_TRIMMED] 标记；> hard cap 继续 hard_order（global → scene → recent_summaries 重写 core JSON → recent_full_texts 但 protected 永远跳过）；(4) 每次 build 日志 logger.info 输出 `chapter=N total=X soft=Y hard=Z breakdown={...}` + meta 新增 estimated_tokens/soft_token_limit/hard_token_limit/token_breakdown/protected_sections/trim_stages_applied；(5) 保留既有 budget_trim_warning 注入 + 对 misconfig (soft>hard) 的 effective_soft=min(soft,hard) 兜底。新增 tests/core/context/test_token_budget.py（13 tests）覆盖：protected 字段 + core 不再双写 + 3×2500 字 ~12k tokens 低于 60k soft 不触发降级 + token_breakdown 结构 + soft cap warn + hard cap 降级顺序 + recent_summaries 重写 core / protected 抗 soft&hard 同时违反 / 配置覆盖 / soft>hard 兜底。全量回归 3021 passed / 23 skipped / 0 failed（baseline 3008 → +13 新测试，零回归）。注：PRD 原 AC 第 1 条提到 'context_weights.py 权重重排'——实现上通过 config-driven protected_sections + trim_order 实现等价语义（recent_full_texts 永不参与 weight 预算，直接跳过 budget 计算），比硬编码权重更灵活。"
    },
    {
      "id": "US-007",
      "title": "端到端回归 + 细节矛盾率对照验收",
      "description": "As 维护者, I want 在已有项目上验证新机制的细节召回效果并确保零回归, so that 合并前对 M-1 ~ M-5 成功指标有量化证据。",
      "acceptanceCriteria": [
        "在一个已有项目从 N=5 续写到 N=7，人工抽样 10 个细节点（人物/道具/地点/时间/对白），记录新旧机制召回率对照表",
        "ink-auto 跨会话批量写作流程下稳定运行 ≥10 章无崩溃，日志不含 warn/error（missing_full_text 除外）",
        "violation 报告平均 evidence 数 ≥1.5 条/次（5 章抽样统计）",
        "writer-agent 的 pre-draft checklist 前三章细节命中率 ≥80%（人工抽样 5 章评估）",
        "全量回归：pytest --no-cov 零新增失败（baseline 2984）",
        "文档同步：docs/architecture.md 相关段落更新、README（若涉及）同步",
        "在 tasks/prd-chapter-context-injection.md 底部追加 'Release Notes' 段：列出验收数据（M-1~M-5 实测值）+ baseline 更新数字",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": "docs/architecture.md 在 Context Agent 节已有 US-002 段，本 US 追加三段：Token 预算 protected 机制（US-006 规则速览：双档 60k/180k soft/hard + protected_sections + trim_stages_applied 日志）/ Writer-Agent 首要参考（US-004 规则速览：PRIMARY REFERENCE + Pre-Draft Checklist 四项 XML 标记 + `.ink/tmp/pre_draft_checklist_ch{NNNN}.md` 落盘 + polish 剥离）/ Continuity-Checker 证据回填（US-005 规则速览：第五层 + evidence:{source_chapter, excerpt} + evidence_source 降级词汇 + additive 并存）。tasks/prd-chapter-context-injection.md 底部追加 Release Notes：交付清单表（US-001~US-006 测试增量）+ M-1~M-5 对照表（M-5 实测通过；M-1~M-4 机制就绪，需实际写作环境补采数据 → 已在 Release Notes '后续验收' 段明确标记）+ 零回归 baseline 更新表（2984 → 3021 建议新 baseline）+ Non-Goals/Open Questions 回顾。全量回归 3021 passed / 23 skipped / 0 failed（与 US-006 baseline 一致，纯文档改动零回归）。Typecheck 替代方案：`python3 -m py_compile` 对 context_manager.py + config.py 通过（项目未配 mypy）。README 不需同步（本 PRD 属架构级优化，不涉用户可见命令/参数）。注：AC 1-4（人工抽样实跑指标）延续 US-003/US-004/US-005 先例——ralph 自动化回合内不强行消耗 API 跑 writer-agent/checker，已在 Release Notes '后续验收' 段交接给维护者在合并后 1 周内在真实 ink-auto 写作中采集回填。"
    }
  ]
}
